import { describe, it } from 'vitest'

describe('liveopticParser', () => {
  it.todo('aggregates totalVcpus from Virtual CPU column for xlsx format')
  it.todo('aggregates totalVcpus from CSV rows')
  it.todo('counts only non-template VMs in vmCount')
  it.todo('computes totalDiskGb from Virtual Disk Size (MiB)')
  it.todo('computes avgRamPerVmGb from Provisioned Memory (MiB)')
})
