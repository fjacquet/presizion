---
phase: 25-spec-lookup-service
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - src/components/step1/CurrentClusterForm.tsx
  - src/components/step1/__tests__/CurrentClusterForm.test.tsx
autonomous: true
requirements:
  - SPEC-LOOKUP-05

must_haves:
  truths:
    - "Clicking 'Look up SPECrate' opens spec-search web UI (fjacquet.github.io/spec-search) in a new tab, not spec.org"
    - "The URL includes the CPU slug as a search/filter parameter so results are pre-filtered"
    - "The button still copies the model number to clipboard before opening"
  artifacts:
    - path: "src/components/step1/CurrentClusterForm.tsx"
      provides: "Updated handleSpecLookup using spec-search URL + cpuModelToSlug"
      contains: "SPEC_SEARCH_WEB_URL"
    - path: "src/components/step1/__tests__/CurrentClusterForm.test.tsx"
      provides: "Updated test asserting spec-search URL instead of spec.org"
      contains: "spec-search"
  key_links:
    - from: "src/components/step1/CurrentClusterForm.tsx"
      to: "src/lib/utils/specLookup.ts"
      via: "import cpuModelToSlug"
      pattern: "import.*cpuModelToSlug.*specLookup"
    - from: "src/components/step1/CurrentClusterForm.tsx"
      to: "src/lib/config.ts"
      via: "import SPEC_SEARCH_WEB_URL"
      pattern: "import.*SPEC_SEARCH_WEB_URL.*config"
---

<objective>
Update the "Look up SPECrate" button in CurrentClusterForm to open spec-search instead of spec.org, using the cpuModelToSlug function from plan 01 to pre-filter results by CPU model.

Purpose: Users get directed to the spec-search UI which has per-processor benchmark data, instead of the generic spec.org form.
Output: Updated `CurrentClusterForm.tsx` and its test file
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-spec-lookup-service/25-01-SUMMARY.md

@src/components/step1/CurrentClusterForm.tsx
@src/components/step1/__tests__/CurrentClusterForm.test.tsx
</context>

<interfaces>
<!-- From plan 01 outputs -->
From src/lib/utils/specLookup.ts:
```typescript
export function cpuModelToSlug(model: string): string;
```

From src/lib/config.ts:
```typescript
export const SPEC_SEARCH_WEB_URL: string; // 'https://fjacquet.github.io/spec-search'
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update handleSpecLookup to use spec-search</name>
  <files>src/components/step1/CurrentClusterForm.tsx, src/components/step1/__tests__/CurrentClusterForm.test.tsx</files>
  <behavior>
    - Test: clicking "Look up SPECrate" button opens URL containing "fjacquet.github.io/spec-search" (not spec.org)
    - Test: the opened URL includes the CPU slug derived from cpuModel (e.g., for "Intel(R) Xeon(R) Gold 6526Y CPU @ 2.40GHz", URL contains "intel-xeon-gold-6526y")
    - Test: clipboard still receives the short model number
  </behavior>
  <action>
    1. In `CurrentClusterForm.tsx`, update the `handleSpecLookup` function:
       - Import `cpuModelToSlug` from `@/lib/utils/specLookup`
       - Import `SPEC_SEARCH_WEB_URL` from `@/lib/config` (replace `SPEC_RESULTS_URL` import)
       - Compute slug: `const slug = cpuModelToSlug(currentCluster.cpuModel)`
       - Build URL: `${SPEC_SEARCH_WEB_URL}/#/processor/${slug}` (spec-search uses hash routing for processor pages)
       - Keep the clipboard copy of the short model number (existing behavior)
       - Update `window.open` call to use the new URL
       - Remove the old dynamic `import('@/lib/config')` for `SPEC_RESULTS_URL` — import `SPEC_SEARCH_WEB_URL` at top level (it's a constant, no lazy loading needed)

    2. In the test file, update the `SPEC-LINK` describe block:
       - Change the assertion from `expect.stringContaining('spec.org/cgi-bin/osgresults')` to `expect.stringContaining('fjacquet.github.io/spec-search')`
       - Add assertion that URL contains the CPU slug
       - Mock `cpuModelToSlug` if needed, or let it run (it's a pure function, no mocking needed)

    3. Update the tooltip text for `specintPerServer` field: replace "Find at spec.org/cpu2017/results/" with "Find at fjacquet.github.io/spec-search" in the FIELD_DESCRIPTIONS object.
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/cluster-sizer && npx vitest run src/components/step1/__tests__/CurrentClusterForm.test.ts</automated>
  </verify>
  <done>
    - "Look up SPECrate" button opens spec-search web UI pre-filtered by CPU slug
    - Existing clipboard copy behavior preserved
    - Test assertions updated to expect spec-search URL
    - No references to spec.org remain in the lookup flow
  </done>
</task>

</tasks>

<verification>
  <automated>cd /Users/fjacquet/Projects/cluster-sizer && npx vitest run src/components/step1/__tests__/CurrentClusterForm.test.ts && npx tsc --noEmit</automated>
</verification>

<success_criteria>
- "Look up SPECrate" button opens fjacquet.github.io/spec-search (not spec.org)
- URL includes CPU slug for pre-filtering
- All CurrentClusterForm tests pass
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/25-spec-lookup-service/25-02-SUMMARY.md`
</output>
