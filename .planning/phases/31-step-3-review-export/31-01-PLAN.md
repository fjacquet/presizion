---
phase: 31-step-3-review-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/step3/ComparisonTable.tsx
  - src/components/step3/__tests__/ComparisonTable.test.tsx
autonomous: true
requirements:
  - REVIEW-01

must_haves:
  truths:
    - "At 390px, the comparison table scrolls horizontally and the Metric column stays fixed"
    - "The Metric column has an opaque background that covers scrolling content behind it"
  artifacts:
    - path: "src/components/step3/ComparisonTable.tsx"
      provides: "Horizontally-scrollable comparison table with sticky first column"
      contains: "min-w-max"
  key_links:
    - from: "ComparisonTable.tsx"
      to: "Table component"
      via: "className min-w-max on Table"
      pattern: "min-w-max"
    - from: "ComparisonTable.tsx first-column cells"
      to: "CSS sticky positioning"
      via: "sticky left-0 bg-background z-10"
      pattern: "sticky left-0"
---

<objective>
Make the Step 3 ComparisonTable horizontally scrollable at 390px with a sticky "Metric" first column.

Purpose: At narrow widths the table currently compresses all columns into the viewport, making scenario data unreadable. Adding `min-w-max` to the `<Table>` forces natural column widths and enables scroll. Sticky positioning on the first column keeps the row labels visible while scrolling.

Output: ComparisonTable.tsx with sticky first column, updated tests confirming CSS classes.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-step-3-review-export/31-RESEARCH.md

@src/components/step3/ComparisonTable.tsx
@src/components/step3/__tests__/ComparisonTable.test.tsx
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/components/ui/table.tsx:
- Table, TableBody, TableCell, TableHead, TableHeader, TableRow (shadcn primitives accepting className prop)

Current ComparisonTable.tsx structure (lines 61-62):
```tsx
<div className="overflow-x-auto rounded-md border">
  <Table>
    // TableHead with className="w-48 font-semibold" for "Metric"
    // TableCell with className="font-medium" for first cells in each row
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add min-w-max to Table and sticky classes to first-column cells</name>
  <files>src/components/step3/ComparisonTable.tsx</files>
  <action>
In ComparisonTable.tsx, make two targeted changes:

1. Add `min-w-max` to the `<Table>` element (line 62):
   ```tsx
   <Table className="min-w-max">
   ```
   This prevents the browser from compressing columns into the viewport, allowing the `overflow-x-auto` wrapper to actually trigger horizontal scroll.

2. Add `sticky left-0 bg-background z-10` to EVERY first-column cell:
   - The `<TableHead>` for "Metric" (line 65): change to `className="w-48 font-semibold sticky left-0 bg-background z-10"`
   - Every `<TableCell className="font-medium">` that is the first cell in a `<TableRow>` in `<TableBody>`. There are 10 of these rows (Servers Required, Server Config, Total pCores, Limiting Resource, vCPU:pCore Ratio, VMs/Server, Headroom, CPU Util %, RAM Util %, Disk Util %).
   - Each gets: `className="font-medium sticky left-0 bg-background z-10"` (preserving any existing classes).

IMPORTANT: `bg-background` is required (not optional) because `position: sticky` does not create a background -- without it the scrolling content shows through the label column. `bg-background` tracks light/dark mode correctly via shadcn CSS variables.

Do NOT change the `overflow-x-auto rounded-md border` wrapper div -- it is already correct.
Do NOT add any responsive breakpoint wrappers -- this CSS applies at all widths (desktop sticky is invisible since no scroll happens).
  </action>
  <verify>
    <automated>rtk vitest run src/components/step3/__tests__/ComparisonTable.test.tsx</automated>
  </verify>
  <done>Table has min-w-max class. All first-column cells (header + body) have sticky left-0 bg-background z-10. Existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add test cases for sticky column classes</name>
  <files>src/components/step3/__tests__/ComparisonTable.test.tsx</files>
  <action>
Add test cases to the existing ComparisonTable test file:

1. **Test: Table element has min-w-max class**
   - Render ComparisonTable with at least one scenario in the store
   - Find the `<table>` element (role="table" or tag name)
   - Assert it has class `min-w-max`

2. **Test: Metric header cell has sticky positioning classes**
   - Render ComparisonTable
   - Find the header cell containing "Metric"
   - Assert it has classes: `sticky`, `left-0`, `bg-background`, `z-10`

3. **Test: First body cells have sticky positioning classes**
   - Render ComparisonTable with at least one scenario
   - Find the cell containing "Servers Required"
   - Assert it has classes: `sticky`, `left-0`, `bg-background`, `z-10`

Use the existing test setup pattern (Zustand store seeding) from the file. The matchMedia mock should already be set up from Phase 29 changes -- if not, add `Object.defineProperty(window, 'matchMedia', ...)` in beforeEach as established in Phase 29.
  </action>
  <verify>
    <automated>rtk vitest run src/components/step3/__tests__/ComparisonTable.test.tsx</automated>
  </verify>
  <done>Three new test cases pass confirming min-w-max on table and sticky classes on first-column cells.</done>
</task>

</tasks>

<verification>
- `rtk vitest run src/components/step3/__tests__/ComparisonTable.test.tsx` -- all tests green
- `rtk vitest run` -- full suite green (no regressions)
- `npx tsc -b` -- no type errors
</verification>

<success_criteria>
- ComparisonTable `<Table>` has `min-w-max` class
- All first-column cells (1 header + 10 body) have `sticky left-0 bg-background z-10`
- Tests verify the sticky classes exist
- No regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/31-step-3-review-export/31-01-SUMMARY.md`
</output>
