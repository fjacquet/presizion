---
phase: 24-average-vm-metrics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/step1/DerivedMetricsPanel.tsx
  - src/components/step1/__tests__/DerivedMetricsPanel.test.tsx
  - src/store/__tests__/useScenariosStore.seed.test.ts
autonomous: true
requirements:
  - AVG-01
  - AVG-02
  - AVG-03
  - AVG-04

must_haves:
  truths:
    - "After import, Step 1 derived metrics panel shows Avg vCPU/VM"
    - "After import, Step 1 derived metrics panel shows Avg RAM/VM (GiB)"
    - "After import, Step 1 derived metrics panel shows Avg Disk/VM (GiB)"
    - "New scenarios added after import inherit RAM/VM and Disk/VM from import data"
  artifacts:
    - path: "src/components/step1/DerivedMetricsPanel.tsx"
      provides: "Three new MetricItem entries for avg vCPU/VM, avg RAM/VM, avg Disk/VM"
      contains: "Avg vCPU/VM"
    - path: "src/components/step1/__tests__/DerivedMetricsPanel.test.tsx"
      provides: "Tests for avg metric display"
      min_lines: 30
    - path: "src/store/__tests__/useScenariosStore.seed.test.ts"
      provides: "Tests verifying seedFromCluster and addScenario seeding"
      min_lines: 20
  key_links:
    - from: "src/components/step1/DerivedMetricsPanel.tsx"
      to: "useClusterStore"
      via: "reads totalVcpus, totalVms, totalDiskGb, avgRamPerVmGb"
      pattern: "cluster\\.totalVcpus.*cluster\\.totalVms"
    - from: "src/store/useScenariosStore.ts"
      to: "OldCluster.avgRamPerVmGb"
      via: "seedFromCluster propagates to scenario ramPerVmGb"
      pattern: "cluster\\.avgRamPerVmGb"
---

<objective>
Display average per-VM resource metrics (vCPU, RAM, Disk) in the Step 1 derived metrics panel after import, and verify that import-derived values seed Step 2 scenario defaults.

Purpose: Presales engineers need to see workload density at a glance and have scenarios auto-populated with real averages instead of generic defaults.
Output: Updated DerivedMetricsPanel with 3 new metrics, plus test coverage for display and seeding.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/step1/DerivedMetricsPanel.tsx
@src/store/useScenariosStore.ts
@src/types/cluster.ts
@src/lib/utils/import/index.ts
@src/components/step2/Step2Scenarios.tsx

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/types/cluster.ts:
```typescript
export interface OldCluster {
  readonly totalVcpus: number;
  readonly totalPcores: number;
  readonly totalVms: number;
  readonly totalDiskGb?: number;
  readonly avgRamPerVmGb?: number;
  // ... other fields
}
```

From src/store/useScenariosStore.ts:
```typescript
seedFromCluster: (cluster: OldCluster) => void;
// Already seeds ramPerVmGb from cluster.avgRamPerVmGb
// Already seeds diskPerVmGb from cluster.totalDiskGb / cluster.totalVms
```

From src/components/step1/DerivedMetricsPanel.tsx:
```typescript
// Currently shows: vCPU:pCore Ratio, VMs/Server (CPU)
// Uses MetricItem({ label, value, unit }) helper
// Grid: grid-cols-2 sm:grid-cols-4
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add avg vCPU/VM, RAM/VM, Disk/VM to DerivedMetricsPanel</name>
  <files>src/components/step1/DerivedMetricsPanel.tsx, src/components/step1/__tests__/DerivedMetricsPanel.test.tsx</files>
  <behavior>
    - Test 1: When cluster has totalVcpus=800 and totalVms=100, displays "8.0" for Avg vCPU/VM
    - Test 2: When cluster has avgRamPerVmGb=12.5, displays "12.5" for Avg RAM/VM with "GiB" unit
    - Test 3: When cluster has totalDiskGb=5000 and totalVms=100, displays "50.0" for Avg Disk/VM with "GiB" unit
    - Test 4: When totalVms is 0, all three avg metrics display em-dash (no division by zero)
    - Test 5: When avgRamPerVmGb is undefined/absent, RAM/VM shows em-dash
  </behavior>
  <action>
    1. Create test file `src/components/step1/__tests__/DerivedMetricsPanel.test.tsx`:
       - Mock `@/store/useClusterStore` to return controlled OldCluster values
       - Mock `@/hooks/useScenariosResults` to return an empty array (not relevant to these metrics)
       - Write tests for each behavior above using `render` + `screen.getByText`

    2. Update `src/components/step1/DerivedMetricsPanel.tsx`:
       - Compute `avgVcpuPerVm`: if `cluster.totalVms > 0`, then `(cluster.totalVcpus / cluster.totalVms).toFixed(1)`, else `'\u2014'`
       - Compute `avgRamPerVm`: if `cluster.avgRamPerVmGb != null`, then `cluster.avgRamPerVmGb.toFixed(1)`, else if `cluster.totalVms > 0` fallback to `'\u2014'` (no raw RAM total on OldCluster), else `'\u2014'`
       - Compute `avgDiskPerVm`: if `cluster.totalDiskGb != null && cluster.totalVms > 0`, then `(cluster.totalDiskGb / cluster.totalVms).toFixed(1)`, else `'\u2014'`
       - Add three `<MetricItem>` entries after existing two:
         - `label="Avg vCPU/VM" value={avgVcpuPerVm}`
         - `label="Avg RAM/VM" value={avgRamPerVm} unit="GiB"`
         - `label="Avg Disk/VM" value={avgDiskPerVm} unit="GiB"`
       - Expand grid to `sm:grid-cols-5` (5 items now: ratio, VMs/server, avg vCPU, avg RAM, avg Disk)
       - Update the footer text to mention "Avg metrics from import data."
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/cluster-sizer && npx vitest run src/components/step1/__tests__/DerivedMetricsPanel.test.tsx --reporter=verbose</automated>
  </verify>
  <done>DerivedMetricsPanel shows 5 metrics total (2 existing + 3 new avg per-VM metrics). All tests pass. Em-dash shown when data is unavailable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify and test scenario seeding from import data</name>
  <files>src/store/__tests__/useScenariosStore.seed.test.ts</files>
  <behavior>
    - Test 1: seedFromCluster with avgRamPerVmGb=12 sets all scenarios' ramPerVmGb to 12
    - Test 2: seedFromCluster with totalDiskGb=5000 and totalVms=100 sets all scenarios' diskPerVmGb to 50
    - Test 3: seedFromCluster with avgRamPerVmGb=undefined does NOT overwrite existing scenario ramPerVmGb
    - Test 4: addScenario reads currentCluster.avgRamPerVmGb and seeds new scenario (verifying Step2Scenarios inline logic)
  </behavior>
  <action>
    1. Create `src/store/__tests__/useScenariosStore.seed.test.ts`:
       - Import `useScenariosStore` and `act` from React testing library or use Zustand's getState/setState directly
       - Test `seedFromCluster` with various OldCluster inputs per behavior list
       - For Test 4, verify the logic in Step2Scenarios.tsx (line 29-36) by unit-testing the computation: `currentCluster.avgRamPerVmGb` flows to `addScenario({ ramPerVmGb })` and `Math.round((totalDiskGb / totalVms) * 10) / 10` flows to `addScenario({ diskPerVmGb })`

    2. No production code changes needed for AVG-04 — the seeding path already works:
       - `seedFromCluster` in useScenariosStore.ts (line 62-92) already propagates `avgRamPerVmGb` and computed `diskPerVmGb`
       - `Step2Scenarios.tsx` (line 29-36) already seeds new scenarios with import-derived values
       - `ImportPreviewModal.tsx` (line 113) calls `seedFromCluster(cluster)` after import confirmation
       - This task confirms via tests that the path is correct
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/cluster-sizer && npx vitest run src/store/__tests__/useScenariosStore.seed.test.ts --reporter=verbose</automated>
  </verify>
  <done>Seeding tests pass, confirming AVG-04: imported avgRamPerVmGb and computed diskPerVmGb flow into scenario defaults via both seedFromCluster and addScenario paths.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/components/step1/__tests__/DerivedMetricsPanel.test.tsx` — all avg metric tests pass
2. `npx vitest run src/store/__tests__/useScenariosStore.seed.test.ts` — all seeding tests pass
3. `npx tsc -b` — no type errors
4. `npx vitest run` — full suite passes (no regressions)
</verification>

<success_criteria>
- DerivedMetricsPanel displays Avg vCPU/VM, Avg RAM/VM (GiB), Avg Disk/VM (GiB) when import data is present
- All three metrics show em-dash when data is unavailable
- Scenario seeding from import is confirmed by test coverage
- Full test suite passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/24-average-vm-metrics/24-01-SUMMARY.md`
</output>
