---
phase: 32-pptx-visual-polish-ux-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/sizing/defaults.ts
  - src/lib/sizing/__tests__/defaults.test.ts
autonomous: true
requirements:
  - UX-01

must_haves:
  truths:
    - "Adding a new scenario from Step 2 produces a card named 'To-Be' by default"
  artifacts:
    - path: "src/lib/sizing/defaults.ts"
      provides: "Default scenario factory with 'To-Be' name"
      contains: "name: 'To-Be'"
    - path: "src/lib/sizing/__tests__/defaults.test.ts"
      provides: "Unit test asserting default name is To-Be"
      contains: "To-Be"
  key_links:
    - from: "src/lib/sizing/defaults.ts"
      to: "src/store/useScenariosStore.ts"
      via: "createDefaultScenario() called when adding scenario"
      pattern: "createDefaultScenario"
---

<objective>
Change the default scenario name from "New Scenario" to "To-Be" and add a unit test for the defaults module.

Purpose: Presales engineers expect the "To-Be" label as standard terminology; eliminates a manual rename step every time a scenario is created.
Output: Updated defaults.ts with "To-Be" name, new defaults.test.ts with assertion.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-pptx-visual-polish-ux-fix/32-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/sizing/defaults.ts:
```typescript
export function createDefaultScenario(): Scenario {
  return {
    id: crypto.randomUUID(),
    name: 'New Scenario',  // <-- change to 'To-Be'
    socketsPerServer: 2,
    coresPerSocket: 16,
    ramPerServerGb: 512,
    diskPerServerGb: 10000,
    targetVcpuToPCoreRatio: DEFAULT_VCPU_TO_PCORE_RATIO,
    ramPerVmGb: 4,
    diskPerVmGb: 50,
    headroomPercent: DEFAULT_HEADROOM_PERCENT,
    haReserveCount: DEFAULT_HA_RESERVE_COUNT,
    targetSpecint: DEFAULT_TARGET_SPECINT,
  };
}
```

From src/types/cluster.ts (Scenario interface):
```typescript
export interface Scenario {
  id: string;
  name: string;
  // ... other fields
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Change default scenario name to "To-Be" and add test</name>
  <files>src/lib/sizing/defaults.ts, src/lib/sizing/__tests__/defaults.test.ts</files>
  <read_first>
    - src/lib/sizing/defaults.ts (the file being modified -- see current `name: 'New Scenario'` at line 59)
    - src/types/cluster.ts (Scenario interface definition)
  </read_first>
  <behavior>
    - Test 1: `createDefaultScenario().name` equals `'To-Be'`
    - Test 2: `createDefaultScenario().id` is a valid UUID string (not empty)
    - Test 3: `createDefaultScenario()` returns an object with all required Scenario fields (socketsPerServer, coresPerSocket, ramPerServerGb, diskPerServerGb, targetVcpuToPCoreRatio, ramPerVmGb, diskPerVmGb, headroomPercent, haReserveCount, targetSpecint)
    - Test 4: Two calls to `createDefaultScenario()` produce different `id` values
  </behavior>
  <action>
1. Create test file `src/lib/sizing/__tests__/defaults.test.ts`:
   - Import `createDefaultScenario` from `../defaults`
   - Import `describe, it, expect` from `vitest`
   - Write four tests as specified in the behavior block above
   - Run tests -- they must FAIL for test 1 (name is still 'New Scenario')

2. Edit `src/lib/sizing/defaults.ts` line 59:
   - Change `name: 'New Scenario',` to `name: 'To-Be',`
   - No other changes to this file

3. Run tests again -- all four tests must PASS
  </action>
  <verify>
    <automated>npx vitest run src/lib/sizing/__tests__/defaults.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/sizing/defaults.ts` contains `name: 'To-Be'` (grep-verifiable)
    - `src/lib/sizing/defaults.ts` does NOT contain `name: 'New Scenario'` (grep-verifiable)
    - `src/lib/sizing/__tests__/defaults.test.ts` exists and contains `expect(scenario.name).toBe('To-Be')`
    - `npx vitest run src/lib/sizing/__tests__/defaults.test.ts` exits 0
    - `npx vitest run` (full suite) exits 0
  </acceptance_criteria>
  <done>createDefaultScenario() returns name "To-Be", all tests green including new defaults test</done>
</task>

</tasks>

<verification>
- `grep "name: 'To-Be'" src/lib/sizing/defaults.ts` returns a match
- `grep "New Scenario" src/lib/sizing/defaults.ts` returns NO match
- `npx vitest run src/lib/sizing/__tests__/defaults.test.ts` passes
- `npx vitest run` full suite passes (no regressions)
</verification>

<success_criteria>
- UX-01 satisfied: new scenarios default to "To-Be"
- Test coverage added for defaults module
- No test regressions across full suite
</success_criteria>

<output>
After completion, create `.planning/phases/32-pptx-visual-polish-ux-fix/32-01-SUMMARY.md`
</output>
