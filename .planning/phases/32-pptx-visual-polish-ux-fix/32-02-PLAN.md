---
phase: 32-pptx-visual-polish-ux-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/utils/exportPptx.ts
  - src/lib/utils/__tests__/exportPptx.test.ts
autonomous: true
requirements:
  - VISUAL-01
  - VISUAL-02
  - VISUAL-03
  - VISUAL-04

must_haves:
  truths:
    - "Every content slide in the exported PPTX has a navy vertical accent strip on the left edge"
    - "Utilization percentage cells display a colored dot (green/amber/red) based on threshold"
    - "All table header rows use navy (#1E3A5F) background instead of blue (#3B82F6)"
    - "All slide titles use navy (#1E3A5F) text instead of blue (#3B82F6)"
    - "KPI callout values appear inside rounded-rectangle shapes with light blue-gray fill"
  artifacts:
    - path: "src/lib/utils/exportPptx.ts"
      provides: "Visually polished PPTX export with accent strip, color-coded util, navy headers, KPI boxes"
      contains: "NAVY"
    - path: "src/lib/utils/__tests__/exportPptx.test.ts"
      provides: "Updated tests verifying visual polish changes"
      contains: "defineSlideMaster"
  key_links:
    - from: "CONTENT_SLIDE master definition"
      to: "All content slides"
      via: "pptx.defineSlideMaster objects array with rect shape"
      pattern: "rect.*NAVY"
    - from: "headerCell() helper"
      to: "All table header rows"
      via: "fill color NAVY propagated to every table"
      pattern: "fill.*NAVY"
    - from: "addKpiCallout() function"
      to: "Executive Summary slide"
      via: "shape: 'roundRect' with rectRadius and fill"
      pattern: "roundRect"
    - from: "utilCell() / utilCellAsIs() helpers"
      to: "Comparison and Summary table slides"
      via: "TextProps[] array with colored dot prepended"
      pattern: "utilColorDot"
---

<objective>
Add four visual polish features to PPTX export: (1) navy accent strip on content slide master, (2) utilization color-coded dots, (3) navy header bands, (4) KPI rounded-rectangle callout boxes.

Purpose: Transform sparse white slides into a professional presales deck that matches corporate presentation standards.
Output: Updated exportPptx.ts with all four visual enhancements, updated tests.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-pptx-visual-polish-ux-fix/32-CONTEXT.md
@.planning/phases/32-pptx-visual-polish-ux-fix/32-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/utils/exportPptx.ts (color constants, lines 19-27):
```typescript
const BLUE = '3B82F6'   // Keep — still used in footer and chart legend
const GREEN = '22C55E'
const AMBER = 'F59E0B'
const DARK = '1F2937'
const GRAY = '6B7280'
const LIGHT_GRAY = 'F3F4F6'
const WHITE = 'FFFFFF'
const FONT = 'Calibri'
const NAVY = '1E3A5F'   // Already defined at line 63
```

From src/lib/utils/exportPptx.ts (current CONTENT_SLIDE master, lines 172-178):
```typescript
pptx.defineSlideMaster({
  title: 'CONTENT_SLIDE',
  background: { color: WHITE },
  objects: [
    { placeholder: { options: { name: 'title', type: 'title', x: 0.5, y: 0.2, w: 12, h: 0.6 }, text: '' } },
  ],
})
```

From src/lib/utils/exportPptx.ts (current helpers, lines 48-95):
```typescript
const TITLE_OPTS = { placeholder: 'title', fontSize: 22, bold: true, color: BLUE, fontFace: FONT } as const
const SUBTITLE_OPTS = { x: 0.5, y: 0.65, w: 12, h: 0.3, fontSize: 11, color: GRAY, fontFace: FONT } as const

function headerCell(text: string) {
  return { text, options: { bold: true, fill: { color: BLUE }, color: WHITE, fontSize: 10, fontFace: FONT } }
}

function addKpiCallout(
  slide: { addText: (t: string, o: Record<string, unknown>) => void },
  items: Array<{ value: string; label: string }>,
  y = 0.9,
) {
  const colW = 12.33 / items.length
  items.forEach((item, i) => {
    slide.addText(item.value, {
      x: 0.5 + i * colW, y, w: colW, h: 0.6,
      fontSize: 44, bold: true, color: BLUE, fontFace: FONT, align: 'center',
    })
    slide.addText(item.label, {
      x: 0.5 + i * colW, y: y + 0.55, w: colW, h: 0.3,
      fontSize: 11, color: GRAY, fontFace: FONT, align: 'center',
    })
  })
}
```

From src/lib/utils/__tests__/exportPptx.test.ts (mock structure, lines 15-35):
```typescript
const mockWriteFile = vi.fn().mockResolvedValue('ok')
const mockAddText = vi.fn().mockReturnThis()
const mockAddTable = vi.fn().mockReturnThis()
const mockAddImage = vi.fn().mockReturnThis()
const mockAddSlide = vi.fn().mockReturnValue({
  addText: mockAddText,
  addTable: mockAddTable,
  addImage: mockAddImage,
})
vi.mock('pptxgenjs', () => {
  class MockPptxGenJS {
    layout = ''
    author = ''
    title = ''
    addSlide = mockAddSlide
    writeFile = mockWriteFile
    defineSlideMaster = vi.fn()
  }
  return { default: MockPptxGenJS }
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add accent strip, navy headers, utilization helpers, and KPI boxes</name>
  <files>src/lib/utils/exportPptx.ts</files>
  <read_first>
    - src/lib/utils/exportPptx.ts (full file -- ~874 lines; understand all BLUE references, master definitions, helpers, and slide content)
    - .planning/phases/32-pptx-visual-polish-ux-fix/32-RESEARCH.md (verified pptxgenjs patterns and pitfalls)
    - .planning/phases/32-pptx-visual-polish-ux-fix/32-CONTEXT.md (locked decisions with exact colors and dimensions)
  </read_first>
  <action>
**Part A -- New color constants and utilization helper (top of file, after existing constants):**

1. Add three new color constants after the existing `NAVY` constant at line 63:
   ```typescript
   const UTIL_GREEN = '22C55E'
   const UTIL_AMBER = 'F59E0B'
   const UTIL_RED = 'EF4444'
   const KPI_FILL = 'E8EDF2'
   ```

2. Add a `utilColorDot(pct: number): string` helper function that returns:
   - `UTIL_GREEN` if `pct < 70`
   - `UTIL_AMBER` if `pct <= 85`
   - `UTIL_RED` otherwise

3. Add a `utilCell(pct: number, rowIdx: number)` helper that returns a table cell object with:
   - `text` as a `TextProps[]` array: `[{ text: '● ', options: { color: utilColorDot(pct), fontSize: 10, fontFace: FONT } }, { text: '${pct.toFixed(1)}%', options: { color: DARK, fontSize: 10, fontFace: FONT } }]`
   - `options: { fill: { color: rowIdx % 2 === 0 ? LIGHT_GRAY : WHITE } }`

4. Add a `plainCell(text: string, rowIdx: number)` helper that returns a simple table cell with alternating row fill (for undefined utilization values).

**Part B -- VISUAL-03: Switch headerCell and TITLE_OPTS from BLUE to NAVY:**

5. Change `TITLE_OPTS` at line 48: replace `color: BLUE` with `color: NAVY`

6. Change `headerCell()` at line 52-54: replace `fill: { color: BLUE }` with `fill: { color: NAVY }`

7. Change ALL inline title `addText` calls that use `color: BLUE` to use `color: NAVY` instead. These are at lines:
   - Line 208: `summarySlide.addText('Executive Summary', ...)` -- change `color: BLUE` to `color: NAVY`
   - Line 257: `comparisonSlide.addText('As-Is vs To-Be Comparison', ...)` -- change `color: BLUE` to `color: NAVY`
   - Line 418: `assumptionsSlide.addText('Sizing Assumptions', ...)` -- change `color: BLUE` to `color: NAVY`
   - Line 563: `growthSlide.addText('Growth Projections', ...)` -- change `color: BLUE` to `color: NAVY`
   - Line 622: `serverConfigSlide.addText('Per-Scenario Server Configuration', ...)` -- change `color: BLUE` to `color: NAVY`
   - Line 674: capacity breakdown slide title -- change `color: BLUE` to `color: NAVY`
   - Line 815: `compSlide.addText('Scenario Comparison', ...)` -- change `color: BLUE` to `color: NAVY`

8. Change ALL inline header row cells that use `fill: { color: BLUE }` to `fill: { color: NAVY }`. These are at lines:
   - Lines 211-215: summary header row cells (5 cells) -- change `fill: { color: BLUE }` to `fill: { color: NAVY }`
   - Lines 297-301: comparison header row cells (Metric, As-Is, scenario names) -- change `fill: { color: BLUE }` to `fill: { color: NAVY }`
   - Lines 625-628: server config header cells -- change `fill: { color: BLUE }` to `fill: { color: NAVY }`
   - Lines 678-682: capacity breakdown header cells (5 cells: Resource, Required, Spare, Excess, Total) -- change `fill: { color: BLUE }` to `fill: { color: NAVY }`
   - Lines 819-822: final comparison header cells -- change `fill: { color: BLUE }` to `fill: { color: NAVY }`

**IMPORTANT: Do NOT change these BLUE references (they are NOT header bands):**
   - Line 71: Footer "Presizion" text color -- keep BLUE (brand accent in footer)
   - Line 729: Chart legend "Required" color -- keep BLUE (chart data color)

**Part C -- VISUAL-01: Add accent strip to CONTENT_SLIDE master:**

9. Update the `CONTENT_SLIDE` master definition (lines 172-178) to add a navy rect shape and shift the title placeholder right:
   ```typescript
   pptx.defineSlideMaster({
     title: 'CONTENT_SLIDE',
     background: { color: WHITE },
     objects: [
       { rect: { x: 0, y: 0, w: 0.3, h: 7.5, fill: { color: NAVY }, line: { color: NAVY } } },
       { placeholder: { options: { name: 'title', type: 'title', x: 0.65, y: 0.2, w: 11.85, h: 0.6 }, text: '' } },
     ],
   })
   ```
   Key changes: add `{ rect: ... }` entry BEFORE the placeholder; shift title placeholder `x` from `0.5` to `0.65` and `w` from `12` to `11.85`.

**Part D -- VISUAL-04: Replace addKpiCallout with shape-background version:**

10. Replace the `addKpiCallout` function (lines 79-95) with a new implementation using rounded-rectangle shape backgrounds:
    ```typescript
    function addKpiCallout(
      slide: { addText: (t: string, o: Record<string, unknown>) => void },
      items: Array<{ value: string; label: string }>,
      y = 0.9,
    ) {
      const colW = 12.33 / items.length
      items.forEach((item, i) => {
        const x = 0.5 + i * colW
        slide.addText(item.value, {
          x, y, w: colW * 0.85, h: 0.75,
          shape: 'roundRect',
          rectRadius: 0.3,
          fill: { color: KPI_FILL },
          fontSize: 44, bold: true, color: NAVY, fontFace: FONT, align: 'center', valign: 'middle',
        })
        slide.addText(item.label, {
          x, y: y + 0.8, w: colW, h: 0.3,
          fontSize: 11, color: GRAY, fontFace: FONT, align: 'center',
        })
      })
    }
    ```
    Changes from current: add `shape: 'roundRect'`, `rectRadius: 0.3`, `fill: { color: KPI_FILL }`, `valign: 'middle'`; change `color: BLUE` to `color: NAVY`; adjust `h` from `0.6` to `0.75`, `w` from `colW` to `colW * 0.85`; adjust label `y` from `y + 0.55` to `y + 0.8`.

**Part E -- VISUAL-02: Apply utilization color-coding to comparison tables:**

11. In the `compMetrics` array (Slide 3: As-Is vs To-Be Comparison), change the data type of `CompMetric` interface:
    - Change `asIs: string` to `asIs: string | { text: unknown[]; options: Record<string, unknown> }`
    - Change `scenarioValues: readonly string[]` to `scenarioValues: readonly (string | { text: unknown[]; options: Record<string, unknown> })[]`
    - Or better: widen the interface to accept mixed cell types (the table cell can be a plain `{ text, options }` object in either form).

12. For the `'CPU Util %'` compMetric entry (around line 366):
    - Change `asIs` to: if `cluster.cpuUtilizationPercent !== undefined`, use `utilCell(cluster.cpuUtilizationPercent, 10)` (row index 10 for CPU Util %); otherwise use `plainCell('--', 10)`
    - Change `scenarioValues` to: `results.map((r) => utilCell(r.cpuUtilizationPercent, 10))`

13. For the `'RAM Util %'` compMetric entry (around line 373):
    - Change `asIs` to: if `cluster.ramUtilizationPercent !== undefined`, use `utilCell(cluster.ramUtilizationPercent, 11)` (row index 11); otherwise use `plainCell('--', 11)`
    - Change `scenarioValues` to: `results.map((r) => utilCell(r.ramUtilizationPercent, 11))`

14. Update `compDataRows` generation (around line 389-399) to handle the new mixed cell types. Instead of always wrapping in `{ text: v, options: ... }`, check if the value is already a cell object (has `.text` and `.options` properties) and pass it through directly. For string values, wrap as before.

15. In the Executive Summary table (Slide 2, around lines 224-226), apply utilization color-coding to the CPU Util % and RAM Util % columns:
    - For each scenario row, replace the CPU util cell `{ text: r ? '${f1(r.cpuUtilizationPercent)}%' : '-', options: ... }` with `r ? utilCell(r.cpuUtilizationPercent, i) : plainCell('-', i)`
    - Same for RAM util cell

16. In the Final Scenario Comparison table (Slide: Scenario Comparison, around lines 833-835), apply utilization color-coding to CPU Util %, RAM Util %, and Disk Util % rows:
    - For the `finalMetrics` array, change the `values` function for `'CPU Util %'`, `'RAM Util %'`, and `'Disk Util %'` entries to return a cell object with TextProps[] instead of a plain string.
    - Update `finalCompRows` generation (around lines 838-850) to handle mixed cell types (string or cell object) the same way as compDataRows.
  </action>
  <verify>
    <automated>npx vitest run src/lib/utils/__tests__/exportPptx.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/utils/exportPptx.ts` contains `const UTIL_GREEN = '22C55E'`
    - `src/lib/utils/exportPptx.ts` contains `const UTIL_AMBER = 'F59E0B'`
    - `src/lib/utils/exportPptx.ts` contains `const UTIL_RED = 'EF4444'`
    - `src/lib/utils/exportPptx.ts` contains `const KPI_FILL = 'E8EDF2'`
    - `src/lib/utils/exportPptx.ts` contains `function utilColorDot`
    - `src/lib/utils/exportPptx.ts` contains `function utilCell`
    - `src/lib/utils/exportPptx.ts` contains `shape: 'roundRect'` (KPI callout)
    - `src/lib/utils/exportPptx.ts` contains `rectRadius: 0.3` (KPI callout)
    - `src/lib/utils/exportPptx.ts` contains `fill: { color: KPI_FILL }` (KPI callout)
    - `src/lib/utils/exportPptx.ts` contains `{ rect:` in CONTENT_SLIDE master definition (accent strip)
    - `src/lib/utils/exportPptx.ts` TITLE_OPTS line contains `color: NAVY` (not `color: BLUE`)
    - `src/lib/utils/exportPptx.ts` headerCell function contains `fill: { color: NAVY }` (not BLUE)
    - `grep -c "fill: { color: BLUE }" src/lib/utils/exportPptx.ts` returns 0 (all header fills changed to NAVY)
    - `grep "color: BLUE" src/lib/utils/exportPptx.ts` matches ONLY footer line (71) and chart legend line (729)
    - `npx vitest run src/lib/utils/__tests__/exportPptx.test.ts` exits 0
  </acceptance_criteria>
  <done>All four VISUAL requirements implemented: accent strip on master, navy headers/titles, utilization color dots, KPI rounded-rect boxes. Existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add targeted tests for VISUAL-01..04 in exportPptx.test.ts</name>
  <files>src/lib/utils/__tests__/exportPptx.test.ts</files>
  <read_first>
    - src/lib/utils/__tests__/exportPptx.test.ts (existing test file -- understand mock structure and existing test patterns)
    - src/lib/utils/exportPptx.ts (the updated implementation from Task 1 -- verify mock requirements)
  </read_first>
  <action>
Add new test cases to the existing `describe('exportPptx', ...)` block in `src/lib/utils/__tests__/exportPptx.test.ts`:

1. **VISUAL-01 test -- accent strip in master definition:**
   ```typescript
   it('defines CONTENT_SLIDE master with navy accent strip rect', async () => {
     const { exportPptx } = await import('../exportPptx')
     await exportPptx(cluster, [scenario], [result], [breakdown], {})
     const pptxInstance = (await import('pptxgenjs')).default
     // defineSlideMaster is called on each instance; check the mock
     const masterCalls = vi.mocked(mockAddSlide).mock.instances
     // Access defineSlideMaster from the mock instance
     // The mock class has defineSlideMaster as vi.fn()
     // We need to check the call args
     const MockPptxGenJS = (await import('pptxgenjs')).default
     const instance = new MockPptxGenJS()
     const dsmCalls = vi.mocked(instance.defineSlideMaster).mock.calls
     // Find the CONTENT_SLIDE call
     const contentCall = dsmCalls.find((c) => c[0]?.title === 'CONTENT_SLIDE')
     expect(contentCall).toBeDefined()
     if (contentCall) {
       const objects = contentCall[0].objects as Array<Record<string, unknown>>
       const rectObj = objects.find((o) => 'rect' in o)
       expect(rectObj).toBeDefined()
       const rect = (rectObj as { rect: { fill: { color: string } } }).rect
       expect(rect.fill.color).toBe('1E3A5F')
     }
   })
   ```
   NOTE: The mock structure may require adjustment. The key approach is to capture the `defineSlideMaster` mock calls and inspect the CONTENT_SLIDE definition for a `rect` entry with `fill.color === '1E3A5F'`. Since `defineSlideMaster` is a `vi.fn()` on the mock class, all instances share the same mock function reference. Access it via the class prototype or directly.

2. **VISUAL-03 test -- navy header fills:**
   ```typescript
   it('uses navy fill in table header cells (not blue)', async () => {
     const { exportPptx } = await import('../exportPptx')
     await exportPptx(cluster, [scenario], [result], [breakdown], {})
     // Inspect addTable calls for header row fill colors
     const tableCalls = mockAddTable.mock.calls as Array<[Array<Array<{ text: string; options?: { fill?: { color: string } } }>>, unknown]>
     // Every first row should have fill.color === '1E3A5F' (NAVY), not '3B82F6' (BLUE)
     const headerFills = tableCalls.flatMap(([rows]) => {
       const firstRow = rows[0]
       return firstRow ? firstRow.map((cell) => cell.options?.fill?.color).filter(Boolean) : []
     })
     // All header fills should be NAVY
     headerFills.forEach((fill) => {
       expect(fill).toBe('1E3A5F')
     })
     // None should be BLUE
     expect(headerFills).not.toContain('3B82F6')
   })
   ```

3. **VISUAL-04 test -- KPI callout with roundRect shape:**
   ```typescript
   it('renders KPI callouts with roundRect shape and fill', async () => {
     const { exportPptx } = await import('../exportPptx')
     await exportPptx(cluster, [scenario], [result], [breakdown], {})
     // Find addText calls with shape: 'roundRect'
     const textCalls = mockAddText.mock.calls as Array<[string, Record<string, unknown>]>
     const kpiCalls = textCalls.filter(([, opts]) => opts?.shape === 'roundRect')
     expect(kpiCalls.length).toBeGreaterThan(0)
     // Check first KPI call has correct props
     const [, firstKpiOpts] = kpiCalls[0]
     expect(firstKpiOpts.rectRadius).toBe(0.3)
     expect(firstKpiOpts.fill).toEqual({ color: 'E8EDF2' })
   })
   ```

4. **VISUAL-02 test -- utilization cells contain colored dot TextProps[]:**
   ```typescript
   it('renders utilization cells with colored dot TextProps array', async () => {
     const { exportPptx } = await import('../exportPptx')
     await exportPptx(cluster, [scenario], [result], [breakdown], {})
     // Find addTable calls where a cell has text as an array (TextProps[])
     const tableCalls = mockAddTable.mock.calls as Array<[unknown[][], unknown]>
     const cellsWithTextArray = tableCalls.flatMap(([rows]) =>
       rows.flatMap((row: unknown[]) =>
         row.filter((cell: unknown) => Array.isArray((cell as { text: unknown }).text))
       )
     )
     // Should have at least one cell with TextProps[] (the utilization cells)
     expect(cellsWithTextArray.length).toBeGreaterThan(0)
     // Check that one of them contains the dot character
     const hasDot = cellsWithTextArray.some((cell: unknown) => {
       const textArr = (cell as { text: Array<{ text: string }> }).text
       return textArr.some((t) => t.text.includes('\u25CF'))  // ● character
     })
     expect(hasDot).toBe(true)
   })
   ```

Ensure all existing tests still pass after adding new ones. The mock structure does not need `addShape` because:
- VISUAL-01 uses `{ rect }` in the master `objects` array (via `defineSlideMaster`, already mocked)
- VISUAL-04 uses `addText` with `shape` prop (via `mockAddText`, already mocked)
  </action>
  <verify>
    <automated>npx vitest run src/lib/utils/__tests__/exportPptx.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/utils/__tests__/exportPptx.test.ts` contains `'navy'` or `'1E3A5F'` in at least one test assertion
    - `src/lib/utils/__tests__/exportPptx.test.ts` contains `'roundRect'` in at least one test assertion
    - `src/lib/utils/__tests__/exportPptx.test.ts` contains `'E8EDF2'` in at least one test assertion (KPI fill)
    - `src/lib/utils/__tests__/exportPptx.test.ts` contains the bullet character test or `TextProps` array assertion
    - `npx vitest run src/lib/utils/__tests__/exportPptx.test.ts` exits 0
    - `npx vitest run` (full suite) exits 0
  </acceptance_criteria>
  <done>Four new tests verify all VISUAL requirements. All existing and new tests pass.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/lib/utils/__tests__/exportPptx.test.ts` -- all tests pass (existing + 4 new)
2. `npx vitest run` -- full suite green (no regressions)
3. `npx tsc -b` -- no TypeScript errors
4. `grep -c "fill: { color: BLUE }" src/lib/utils/exportPptx.ts` returns 0 (no BLUE header fills remain)
5. `grep "color: BLUE" src/lib/utils/exportPptx.ts | wc -l` returns exactly 2 (footer + chart legend only)
6. `grep "shape: 'roundRect'" src/lib/utils/exportPptx.ts` returns at least 1 match
7. `grep "{ rect:" src/lib/utils/exportPptx.ts` returns at least 1 match (master accent strip)
</verification>

<success_criteria>
- VISUAL-01: CONTENT_SLIDE master includes 0.3"-wide navy accent strip rect at x=0, full height
- VISUAL-02: Utilization cells in summary, comparison, and scenario comparison tables use colored-dot TextProps[]
- VISUAL-03: All table headers use navy (#1E3A5F) fill; all slide titles use navy text color
- VISUAL-04: KPI callouts use addText with shape='roundRect', rectRadius=0.3, fill=#E8EDF2
- All tests pass, no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/32-pptx-visual-polish-ux-fix/32-02-SUMMARY.md`
</output>
