---
phase: 06-conditional-ui-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/wizard/__tests__/SizingModeToggle.test.tsx
  - src/components/step3/__tests__/ComparisonTable.test.tsx
  - src/components/wizard/SizingModeToggle.tsx
  - src/components/wizard/WizardShell.tsx
autonomous: true
requirements:
  - PERF-02
  - PERF-03

must_haves:
  truths:
    - "A visible vCPU / SPECint toggle exists in the WizardShell header on every step"
    - "Clicking SPECint sets sizingMode to 'specint' in useWizardStore (aria-pressed updates)"
    - "Clicking vCPU sets sizingMode to 'vcpu' in useWizardStore"
    - "SizingModeToggle.test.tsx exists with it.todo stubs for all toggle behaviors"
    - "ComparisonTable.test.tsx exists with it.todo stub for SPECint label correctness"
    - "Vitest suite exits 0 (all it.todo are pending, not failing)"
  artifacts:
    - path: "src/components/wizard/SizingModeToggle.tsx"
      provides: "vCPU / SPECint mode toggle rendered in WizardShell header"
      exports: ["SizingModeToggle"]
    - path: "src/components/wizard/__tests__/SizingModeToggle.test.tsx"
      provides: "Wave 0 it.todo stubs for toggle visibility, aria-pressed, setSizingMode"
    - path: "src/components/step3/__tests__/ComparisonTable.test.tsx"
      provides: "Wave 0 it.todo stub for SPECint label correctness in limiting resource column"
    - path: "src/components/wizard/WizardShell.tsx"
      provides: "WizardShell with SizingModeToggle wired into header"
  key_links:
    - from: "src/components/wizard/WizardShell.tsx"
      to: "src/components/wizard/SizingModeToggle.tsx"
      via: "import and render in header"
      pattern: "SizingModeToggle"
    - from: "src/components/wizard/SizingModeToggle.tsx"
      to: "src/store/useWizardStore.ts"
      via: "useWizardStore selector"
      pattern: "useWizardStore.*sizingMode|setSizingMode"
---

<objective>
Plant Wave 0 test stubs for the two new test files needed this phase, then create the SizingModeToggle component and wire it into WizardShell. The toggle is the global entry point for PERF-02 and PERF-03 — without it, users cannot activate SPECint mode.

Purpose: Establish test scaffolding (Nyquist compliance) and deliver the mode toggle UI so all subsequent conditional field work in Plan 02 has a functioning trigger.
Output: SizingModeToggle component in WizardShell header; two Wave 0 test files with it.todo stubs; vitest suite exits 0.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-conditional-ui-wiring/06-RESEARCH.md

@src/store/useWizardStore.ts
@src/components/wizard/WizardShell.tsx
</context>

<interfaces>
<!-- Key types from src/store/useWizardStore.ts -->
```typescript
export type SizingMode = 'vcpu' | 'specint';

interface WizardStore {
  currentStep: number;
  sizingMode: SizingMode;
  setSizingMode: (mode: SizingMode) => void;
  nextStep: () => void;
  prevStep: () => void;
}

export const useWizardStore = create<WizardStore>((set) => ({ ... }))
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 test stubs — SizingModeToggle and ComparisonTable</name>
  <files>
    src/components/wizard/__tests__/SizingModeToggle.test.tsx
    src/components/step3/__tests__/ComparisonTable.test.tsx
  </files>
  <behavior>
    SizingModeToggle.test.tsx stubs:
    - it.todo: 'renders vCPU button with aria-pressed=true when sizingMode is vcpu'
    - it.todo: 'renders SPECint button with aria-pressed=true when sizingMode is specint'
    - it.todo: 'clicking SPECint button calls setSizingMode with specint'
    - it.todo: 'clicking vCPU button calls setSizingMode with vcpu'
    - it.todo: 'toggle is visible in WizardShell header'

    ComparisonTable.test.tsx stubs (PERF-03 SC-3):
    - it.todo: 'shows SPECint (not Specint) in limiting resource column when specint mode active'
    - it.todo: 'shows CPU-limited label when cpu is limiting resource'

    Both files must have correct imports and describe blocks but all tests use it.todo.
    NO it.skip — it.todo is required (exits 0, counts as pending not failing).
  </behavior>
  <action>
    Create src/components/wizard/__tests__/SizingModeToggle.test.tsx with:
    - imports: describe, it from 'vitest'
    - describe('SizingModeToggle', () => { all stubs as it.todo })

    Create src/components/step3/__tests__/ComparisonTable.test.tsx with:
    - imports: describe, it from 'vitest'
    - describe('ComparisonTable', () => { all stubs as it.todo })

    Note: No implementation imports yet — just the describe/it.todo skeleton.
    This is Wave 0: stubs exist so the test runner counts them as pending (not failing).
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/cluster-sizer && rtk vitest run src/components/wizard/__tests__/SizingModeToggle.test.tsx src/components/step3/__tests__/ComparisonTable.test.tsx</automated>
  </verify>
  <done>Both test files exist; vitest reports them as "todo" (pending); exit code 0</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SizingModeToggle component + WizardShell wiring + fill toggle tests</name>
  <files>
    src/components/wizard/SizingModeToggle.tsx
    src/components/wizard/WizardShell.tsx
    src/components/wizard/__tests__/SizingModeToggle.test.tsx
  </files>
  <behavior>
    SizingModeToggle tests (fill from it.todo):
    - renders vCPU button with aria-pressed="true" when sizingMode is 'vcpu'
    - renders SPECint button with aria-pressed="false" when sizingMode is 'vcpu'
    - renders SPECint button with aria-pressed="true" when sizingMode is 'specint'
    - clicking SPECint button calls setSizingMode('specint')
    - clicking vCPU button calls setSizingMode('vcpu')

    Component behavior:
    - Role group with aria-label="Sizing mode"
    - Two buttons: "vCPU" and "SPECint"
    - aria-pressed reflects current sizingMode from useWizardStore
    - Active button: font-semibold + underline classes; inactive: text-muted-foreground
    - Both buttons have type="button" (prevent form submission)
    - WizardShell renders SizingModeToggle in the header, below the subtitle paragraph
  </behavior>
  <action>
    1. Create src/components/wizard/SizingModeToggle.tsx:
       - Import useWizardStore and SizingMode type from '@/store/useWizardStore'
       - Read sizingMode and setSizingMode via selector pattern:
         const sizingMode = useWizardStore((s) => s.sizingMode)
         const setSizingMode = useWizardStore((s) => s.setSizingMode)
       - Render a div with role="group" aria-label="Sizing mode" and flex gap-1 text-sm classes
       - Two buttons (type="button"):
         * "vCPU" — aria-pressed={sizingMode === 'vcpu'} onClick={() => setSizingMode('vcpu')}
         * Separator span: "/"
         * "SPECint" — aria-pressed={sizingMode === 'specint'} onClick={() => setSizingMode('specint')}
       - Active button className: "font-semibold underline"
       - Inactive button className: "text-muted-foreground"
       - Export as named export: export function SizingModeToggle()

    2. Modify src/components/wizard/WizardShell.tsx:
       - Import SizingModeToggle from './SizingModeToggle'
       - Add <SizingModeToggle /> inside the <header> element, after the subtitle <p> tag
       - No other changes to WizardShell

    3. Fill SizingModeToggle.test.tsx:
       - Replace it.todo stubs with real tests
       - Mock useWizardStore using vi.mock('@/store/useWizardStore', ...) pattern
       - Test aria-pressed state and click handlers via @testing-library/react
       - Remove the "toggle is visible in WizardShell header" stub (visual layout — manual only per VALIDATION.md)
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/cluster-sizer && rtk vitest run src/components/wizard/__tests__/SizingModeToggle.test.tsx</automated>
  </verify>
  <done>
    SizingModeToggle.tsx exists and exports SizingModeToggle function;
    WizardShell.tsx imports and renders it in the header;
    All SizingModeToggle tests pass (not todo);
    vitest exits 0 with full suite green
  </done>
</task>

</tasks>

<verification>
Full suite check after plan completes:
  cd /Users/fjacquet/Projects/cluster-sizer && rtk vitest run

Expected: All pre-existing tests still pass (205+), SizingModeToggle tests pass, ComparisonTable stubs show as pending.
TypeScript build check:
  cd /Users/fjacquet/Projects/cluster-sizer && npm run build 2>&1 | tail -10
Expected: zero TypeScript errors.
</verification>

<success_criteria>
- SizingModeToggle renders vCPU / SPECint buttons with correct aria-pressed state
- Clicking either button updates sizingMode in useWizardStore
- WizardShell header contains SizingModeToggle visible on all 3 wizard steps
- src/components/wizard/__tests__/SizingModeToggle.test.tsx: all tests pass
- src/components/step3/__tests__/ComparisonTable.test.tsx: stubs pending (it.todo), exit 0
- Full vitest suite exits 0; npm run build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/06-conditional-ui-wiring/06-01-SUMMARY.md`
</output>
