---
phase: 26-spec-lookup-ui
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - src/components/step2/ScenarioCard.tsx
  - src/components/step2/__tests__/ScenarioCard.test.tsx
autonomous: true
requirements: [SPEC-LOOKUP-04]

must_haves:
  truths:
    - "Step 2 ScenarioCard has a text input where user can type a target CPU model"
    - "After typing a CPU model, SPEC results appear in a collapsible panel"
    - "Clicking a result row auto-fills targetSpecint with the selected base score"
    - "When lookup returns no results, a fallback message appears"
    - "The manual targetSpecint input field remains accessible at all times"
  artifacts:
    - path: "src/components/step2/ScenarioCard.tsx"
      provides: "ScenarioCard with SPEC lookup for target CPU"
  key_links:
    - from: "src/components/step2/ScenarioCard.tsx"
      to: "src/hooks/useSpecLookup.ts"
      via: "useSpecLookup hook for target CPU model"
      pattern: "useSpecLookup"
    - from: "src/components/step2/ScenarioCard.tsx"
      to: "src/components/common/SpecResultsPanel.tsx"
      via: "renders SpecResultsPanel with onSelect wiring to targetSpecint"
      pattern: "SpecResultsPanel.*onSelect"
---

<objective>
Wire the reusable SpecResultsPanel into Step 2 (ScenarioCard) so users can search for a target CPU model and click a benchmark result to auto-fill targetSpecint.

Purpose: Eliminates manual SPEC website lookup for target server CPU — users type a model, see results, click to fill.
Output: Updated ScenarioCard with CPU model search input and SPEC results panel in SPECint mode.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-spec-lookup-ui/26-01-PLAN.md

<interfaces>
<!-- From Plan 01 (created in this phase) -->

From src/hooks/useSpecLookup.ts:
```typescript
export function useSpecLookup(cpuModel: string | undefined): {
  results: SpecResult[]
  status: 'ok' | 'no-results' | 'error'
  isLoading: boolean
}
```

From src/components/common/SpecResultsPanel.tsx:
```typescript
interface SpecResultsPanelProps {
  results: SpecResult[]
  status: 'ok' | 'no-results' | 'error'
  isLoading: boolean
  onSelect: (baseScore: number) => void
  selectedScore?: number
}
export function SpecResultsPanel(props: SpecResultsPanelProps): JSX.Element
```

From src/types/cluster.ts (Scenario):
```typescript
readonly targetSpecint?: number;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add target CPU model search and SpecResultsPanel to ScenarioCard</name>
  <files>
    src/components/step2/ScenarioCard.tsx,
    src/components/step2/__tests__/ScenarioCard.test.tsx
  </files>
  <behavior>
    - In SPECint mode, a text input labeled "Target CPU Model" appears above the targetSpecint field
    - Typing a CPU model string (e.g., "Xeon Gold 6526Y") triggers SPEC lookup after debounce
    - Results panel appears below the input showing matching SPEC results
    - Clicking a result row fills targetSpecint with the selected base score
    - When not in SPECint mode, no CPU model input or results panel is shown
    - The targetSpecint manual input remains functional even when panel is shown
  </behavior>
  <action>
    1. In `ScenarioCard.tsx`, inside the `sizingMode === 'specint'` section:
       - Add local state: `const [targetCpuModel, setTargetCpuModel] = useState('')`
       - Add a debounced version: use a simple `useEffect` with a 500ms `setTimeout` to set `debouncedCpuModel` state from `targetCpuModel`. This avoids fetching on every keystroke.
       - Call `useSpecLookup(debouncedCpuModel || undefined)` to get results/status/isLoading
       - Track `selectedTargetScore` in local state (number | undefined)
       - Render a plain text `<Input>` for "Target CPU Model" with `placeholder="e.g. Xeon Gold 6526Y"` ABOVE the existing targetSpecint Controller
       - Render `<SpecResultsPanel>` between the CPU model input and the targetSpecint input
       - The `onSelect` callback: sets `selectedTargetScore` state AND calls `form.setValue('targetSpecint', baseScore, { shouldValidate: true })` (using the Controller's field.onChange pattern)

    2. Import `useSpecLookup` from `@/hooks/useSpecLookup` and `SpecResultsPanel` from `@/components/common/SpecResultsPanel`

    3. Update/add tests:
       - Mock `fetchSpecResults` to return sample results
       - Test: in SPECint mode, "Target CPU Model" input renders
       - Test: typing a CPU model and waiting for debounce triggers fetch, results panel appears
       - Test: clicking a result row updates targetSpecint field
       - Test: in non-SPECint mode (e.g. vcpu), no "Target CPU Model" input renders

    IMPORTANT: Do NOT add targetCpuModel to the Scenario type or Zod schema. It is purely local UI state — per the derive-on-read pattern, transient search queries are not persisted.
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/cluster-sizer && npx vitest run src/components/step2/__tests__/ScenarioCard.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>In SPECint mode, ScenarioCard shows target CPU model input + SPEC results panel; clicking a result fills targetSpecint; non-SPECint modes unaffected; all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Full regression check and build verification</name>
  <files></files>
  <action>
    1. Run the full test suite: `npx vitest run` — all tests must pass
    2. Run TypeScript build check: `npx tsc -b` — no errors
    3. Run lint: `npx eslint src/` — no new warnings/errors
    4. If any failures, fix them before completing

    This task ensures no regressions from the Step 1 and Step 2 integrations.
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/cluster-sizer && npx vitest run 2>&1 | tail -10 && npx tsc -b 2>&1 | tail -5</automated>
  </verify>
  <done>All project tests pass, TypeScript compiles cleanly, no lint errors</done>
</task>

</tasks>

<verification>
- `npx vitest run` — all project tests pass (no regressions)
- `npx tsc -b` — no TypeScript errors
- In SPECint mode, ScenarioCard shows CPU model search + results panel
- Clicking a result fills targetSpecint
- In non-SPECint modes, no SPEC lookup UI visible in ScenarioCard
</verification>

<success_criteria>
- SPEC-LOOKUP-04: Step 2 ScenarioCard has SPEC lookup for target CPU with search input and auto-fill
- Debounced input prevents excessive API calls
- Manual targetSpecint entry remains fully functional
- No regressions across full test suite and build
</success_criteria>

<output>
After completion, create `.planning/phases/26-spec-lookup-ui/26-02-SUMMARY.md`
</output>
