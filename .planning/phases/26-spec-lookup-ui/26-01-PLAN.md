---
phase: 26-spec-lookup-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useSpecLookup.ts
  - src/components/common/SpecResultsPanel.tsx
  - src/components/step1/CurrentClusterForm.tsx
  - src/hooks/__tests__/useSpecLookup.test.ts
  - src/components/common/__tests__/SpecResultsPanel.test.tsx
autonomous: true
requirements: [SPEC-LOOKUP-02, SPEC-LOOKUP-03]

must_haves:
  truths:
    - "After import with a recognized CPU model, a SPECrate results panel appears in Step 1"
    - "The panel shows vendor, system name, base score, cores, and chips for each result"
    - "Clicking a result row auto-fills specintPerServer with the base score"
    - "When no CPU model is detected or lookup returns no results, a fallback message appears"
    - "The manual specintPerServer input field remains accessible at all times"
  artifacts:
    - path: "src/hooks/useSpecLookup.ts"
      provides: "useSpecLookup custom hook wrapping fetchSpecResults with React state"
      exports: ["useSpecLookup"]
    - path: "src/components/common/SpecResultsPanel.tsx"
      provides: "Reusable collapsible panel displaying SPEC benchmark results"
      exports: ["SpecResultsPanel"]
    - path: "src/components/step1/CurrentClusterForm.tsx"
      provides: "Step 1 form with integrated SPEC results panel"
  key_links:
    - from: "src/hooks/useSpecLookup.ts"
      to: "src/lib/utils/specLookup.ts"
      via: "imports cpuModelToSlug + fetchSpecResults"
      pattern: "fetchSpecResults.*cpuModelToSlug"
    - from: "src/components/common/SpecResultsPanel.tsx"
      to: "src/hooks/useSpecLookup.ts"
      via: "receives results from hook"
      pattern: "SpecResult"
    - from: "src/components/step1/CurrentClusterForm.tsx"
      to: "src/components/common/SpecResultsPanel.tsx"
      via: "renders panel with onSelect callback"
      pattern: "SpecResultsPanel"
---

<objective>
Create a reusable SPEC results panel component and custom hook, then wire them into Step 1 (CurrentClusterForm) so that after import with a recognized CPU model, users see benchmark results and can click to auto-fill specintPerServer.

Purpose: Eliminates manual SPEC website lookup for existing cluster CPU — users see results inline and click to fill.
Output: useSpecLookup hook, SpecResultsPanel component, updated CurrentClusterForm with integrated panel.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-spec-lookup-service/25-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/utils/specLookup.ts:
```typescript
export interface SpecResult {
  vendor: string
  system: string
  baseResult: number
  peakResult: number
  cores: number
  chips: number
}

export interface SpecLookupResult {
  results: SpecResult[]
  status: 'ok' | 'no-results' | 'error'
}

export function cpuModelToSlug(model: string): string
export function fetchSpecResults(slug: string): Promise<SpecLookupResult>
```

From src/lib/config.ts:
```typescript
export const SPEC_SEARCH_API_URL = 'https://fjacquet.github.io/spec-search'
export const SPEC_SEARCH_WEB_URL = 'https://fjacquet.github.io/spec-search'
```

From src/types/cluster.ts (OldCluster):
```typescript
readonly cpuModel?: string;
readonly specintPerServer?: number;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create useSpecLookup hook and SpecResultsPanel component</name>
  <files>
    src/hooks/useSpecLookup.ts,
    src/hooks/__tests__/useSpecLookup.test.ts,
    src/components/common/SpecResultsPanel.tsx,
    src/components/common/__tests__/SpecResultsPanel.test.tsx
  </files>
  <behavior>
    - useSpecLookup(cpuModel: string | undefined): when cpuModel changes to a non-empty string, auto-fetches via cpuModelToSlug + fetchSpecResults; returns { results: SpecResult[], status, isLoading }
    - useSpecLookup returns isLoading=true during fetch, false after
    - useSpecLookup with undefined/empty cpuModel returns empty results and 'no-results' status without fetching
    - SpecResultsPanel renders a collapsible section (collapsed by default) with heading "SPECrate2017 Results"
    - SpecResultsPanel shows a table with columns: Vendor, System, Base Score, Cores, Chips
    - SpecResultsPanel with status='no-results' shows "No SPECrate2017 results found for this CPU model"
    - SpecResultsPanel with status='error' shows "Could not fetch SPEC results. Use manual entry below."
    - SpecResultsPanel with isLoading=true shows a "Loading..." indicator
    - Clicking a result row calls onSelect(baseResult) callback
    - Selected row is visually highlighted
  </behavior>
  <action>
    1. Create `src/hooks/useSpecLookup.ts`:
       - Accept `cpuModel: string | undefined`
       - Use `useState` for results/status/isLoading, `useEffect` to trigger fetch when cpuModel changes
       - Derive slug via `cpuModelToSlug(cpuModel)`, skip fetch if slug is empty
       - Call `fetchSpecResults(slug)` and update state
       - Return `{ results, status, isLoading }`

    2. Create `src/components/common/SpecResultsPanel.tsx`:
       - Props: `results: SpecResult[]`, `status: SpecLookupResult['status']`, `isLoading: boolean`, `onSelect: (baseScore: number) => void`, `selectedScore?: number`
       - Use a `<details>` element or a simple collapsible div with chevron toggle (use Tailwind, no new deps)
       - Render a compact table: Vendor | System | Base Score | Cores | Chips
       - Each row has `cursor-pointer` and `hover:bg-muted`, clicking calls `onSelect(row.baseResult)`
       - When `selectedScore === row.baseResult`, highlight row with `bg-primary/10`
       - Show fallback messages for no-results, error, and loading states
       - Do NOT use shadcn Table component — use plain HTML table with Tailwind for simplicity

    3. Write tests:
       - Hook test: mock fetchSpecResults, verify results/status/isLoading transitions
       - Panel test: render with sample results, verify table rows, verify onSelect fires on click, verify fallback messages for each status
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/cluster-sizer && npx vitest run src/hooks/__tests__/useSpecLookup.test.ts src/components/common/__tests__/SpecResultsPanel.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>useSpecLookup hook auto-fetches SPEC results when cpuModel changes; SpecResultsPanel renders results table with click-to-select; all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Wire SpecResultsPanel into CurrentClusterForm (Step 1)</name>
  <files>
    src/components/step1/CurrentClusterForm.tsx,
    src/components/step1/__tests__/CurrentClusterForm.test.tsx
  </files>
  <action>
    1. In `CurrentClusterForm.tsx`:
       - Import `useSpecLookup` and `SpecResultsPanel`
       - Call `useSpecLookup(currentCluster.cpuModel)` to get results/status/isLoading
       - Track `selectedSpecScore` in local state (number | undefined)
       - Place `<SpecResultsPanel>` immediately after the "Look up SPECrate" button (inside the "Existing Server Config" section), passing results/status/isLoading/onSelect/selectedScore
       - The `onSelect` callback: sets `selectedSpecScore` state AND calls `form.setValue('specintPerServer', baseScore, { shouldValidate: true })` to auto-fill the field
       - The panel should only render when `currentCluster.cpuModel` is truthy (same guard as the "Detected CPU" badge)

    2. Update tests in `CurrentClusterForm.test.tsx`:
       - Mock `fetchSpecResults` to return sample data when cpuModel is set
       - Test: when cpuModel is present, panel renders with results
       - Test: clicking a result row updates the specintPerServer field value
       - Test: when cpuModel is absent, panel does not render

    IMPORTANT: Keep the existing "Look up SPECrate" external link button — the panel is an addition, not a replacement.
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/cluster-sizer && npx vitest run src/components/step1/__tests__/CurrentClusterForm.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>Step 1 shows SPEC results panel after import with CPU model; clicking a row fills specintPerServer; existing tests still pass; manual entry remains accessible</done>
</task>

</tasks>

<verification>
- `npx vitest run` — all project tests pass (no regressions)
- `npx tsc -b` — no TypeScript errors
- Step 1 with imported CPU model shows collapsible SPEC results panel
- Clicking a result fills specintPerServer field
- Without CPU model, no panel renders
</verification>

<success_criteria>
- SPEC-LOOKUP-02: Results panel shows vendor, system, base score, cores, chips
- SPEC-LOOKUP-03: Click fills specintPerServer in Step 1
- Panel handles loading, no-results, and error states with clear messages
- Manual specintPerServer entry remains fully functional
</success_criteria>

<output>
After completion, create `.planning/phases/26-spec-lookup-ui/26-01-SUMMARY.md`
</output>
