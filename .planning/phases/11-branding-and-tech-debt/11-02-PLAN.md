---
phase: 11-branding-and-tech-debt
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/logo.svg
  - public/favicon.svg
  - src/components/wizard/WizardShell.tsx
autonomous: false
requirements:
  - BRAND-01
  - BRAND-02

must_haves:
  truths:
    - "The app header displays the Presizion logo (SVG image) alongside or above the heading text"
    - "The browser tab shows a custom Presizion favicon (geometric, blue/slate palette) instead of the Vite default purple bolt"
    - "The logo uses the blue/slate palette (#3B82F6 blue, #475569 slate) as specified"
    - "The favicon.svg file is a recognizable Presizion mark (geometric 'P' or wordmark)"
  artifacts:
    - path: "public/logo.svg"
      provides: "Presizion wordmark or 'P' monogram, blue/slate palette, inline SVG"
    - path: "public/favicon.svg"
      provides: "Compact Presizion mark for tab favicon, blue/slate palette"
    - path: "src/components/wizard/WizardShell.tsx"
      provides: "Header with <img> tag rendering logo.svg"
      contains: "logo.svg"
  key_links:
    - from: "src/components/wizard/WizardShell.tsx"
      to: "public/logo.svg"
      via: "<img src=\"/presizion/logo.svg\" alt=\"Presizion\" />"
      pattern: "presizion/logo.svg"
    - from: "index.html"
      to: "public/favicon.svg"
      via: "<link rel=\"icon\" type=\"image/svg+xml\" href=\"/favicon.svg\" />"
      pattern: "favicon.svg"
---

<objective>
Create Presizion brand assets (logo SVG and favicon SVG) and integrate the logo into the WizardShell header, replacing the Vite default assets.

Purpose: The app currently shows generic Vite branding. Presizion identity assets establish product credibility with presales engineers.
Output: logo.svg (header), favicon.svg (tab), WizardShell header updated.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key contracts the executor needs. No codebase exploration required. -->

Brand palette (locked decisions):
- Primary blue: #3B82F6 (Tailwind blue-500)
- Slate: #475569 (Tailwind slate-600)
- Accent lighter blue: #93C5FD (Tailwind blue-300, for gradients/highlights)
- Background: white (#FFFFFF) for light mode compatibility

Base path (from vite.config.ts):
- base: '/presizion/'
- Therefore logo src must be: /presizion/logo.svg (absolute from domain root)

Current index.html favicon reference (no change needed — filename stays favicon.svg):
```html
<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
```
Note: index.html uses "/favicon.svg" (no base path prefix for favicon — correct for Vite static assets).

Current WizardShell.tsx header (to be updated):
```tsx
<header className="mb-6 text-center print:hidden">
  <h1 className="text-2xl font-bold tracking-tight">Cluster Refresh Sizing</h1>
  <p className="text-sm text-muted-foreground mt-1">
    Size your refreshed cluster based on existing metrics
  </p>
  <SizingModeToggle />
</header>
```

Current favicon.svg content: Vite default purple bolt SVG (48×46px, fill:#863bff).
This file must be completely replaced with the Presizion mark.

WizardShell.tsx component rules:
- Function component, ≤150 lines (currently 53 lines — ample room)
- No `any` TypeScript
- Use img tag (not SVG inline in JSX) to keep component size small
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Presizion logo.svg and replace favicon.svg</name>
  <files>public/logo.svg, public/favicon.svg</files>
  <action>
    Create public/logo.svg — Presizion wordmark or "P" monogram, blue/slate palette:
    Design a clean geometric SVG logo. Use a horizontal layout: geometric "P" mark on the left + "Presizion" wordmark on the right, or a standalone "P" geometric mark if preferred. Requirements:
    - viewBox="0 0 160 40" for wordmark layout (or "0 0 40 40" for monogram-only)
    - Primary color: #3B82F6 (blue) for the mark/accent elements
    - Text color: #475569 (slate) for wordmark text
    - Clean geometric style — avoid decorative flourishes
    - The "P" mark suggestion: two stacked horizontal rectangles (top bar + descender) forming a geometric "P" shape, with a blue filled circle or rectangle as accent
    - If creating a wordmark: use font-family="system-ui, -apple-system, sans-serif" font-weight="600"
    - Example minimal "P" monogram structure:
      ```svg
      <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 160 40" fill="none">
        <!-- Geometric P mark (left, 40x40 area) -->
        <rect x="4" y="6" width="6" height="28" fill="#3B82F6" rx="2"/>
        <rect x="4" y="6" width="20" height="6" fill="#3B82F6" rx="2"/>
        <rect x="18" y="6" width="6" height="14" fill="#3B82F6" rx="2"/>
        <rect x="4" y="18" width="20" height="6" fill="#3B82F6" rx="2"/>
        <!-- Wordmark (right, starting at x=46) -->
        <text x="46" y="28" font-family="system-ui,-apple-system,sans-serif"
              font-size="22" font-weight="600" fill="#475569">Presizion</text>
      </svg>
      ```
    Adjust coordinates to look balanced. The executor has discretion on exact geometry as long as it uses the specified palette and is recognizable as a geometric "P" + wordmark.

    Replace public/favicon.svg — compact Presizion mark for browser tab:
    The favicon should be the geometric "P" mark only (no wordmark text, as it would be unreadable at 16x16).
    - viewBox="0 0 32 32"
    - Same blue #3B82F6 fill
    - Geometric "P" mark centered in the 32x32 viewport
    - Example:
      ```svg
      <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" fill="none">
        <rect width="32" height="32" rx="6" fill="#3B82F6"/>
        <rect x="8" y="6" width="4" height="20" fill="white" rx="1"/>
        <rect x="8" y="6" width="14" height="4" fill="white" rx="1"/>
        <rect x="20" y="6" width="4" height="10" fill="white" rx="1"/>
        <rect x="8" y="14" width="16" height="4" fill="white" rx="1"/>
      </svg>
      ```
    Adjust geometry for visual balance. Blue background with white "P" reads clearly at small sizes.

    IMPORTANT: Both SVG files must be complete, valid, self-contained SVG files with proper xmlns declaration.
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/cluster-sizer && test -f public/logo.svg && test -f public/favicon.svg && grep -q "3B82F6" public/logo.svg && grep -q "3B82F6" public/favicon.svg && echo "Assets OK"</automated>
  </verify>
  <done>public/logo.svg exists with #3B82F6 in it. public/favicon.svg replaced with Presizion mark containing #3B82F6. Both are valid SVG files with xmlns declaration.</done>
</task>

<task type="auto">
  <name>Task 2: Add Presizion logo to WizardShell header</name>
  <files>src/components/wizard/WizardShell.tsx</files>
  <action>
    Update the header in WizardShell.tsx to display the Presizion logo above the heading text.

    Replace the existing header content:
    ```tsx
    &lt;header className="mb-6 text-center print:hidden"&gt;
      &lt;h1 className="text-2xl font-bold tracking-tight"&gt;Cluster Refresh Sizing&lt;/h1&gt;
      &lt;p className="text-sm text-muted-foreground mt-1"&gt;
        Size your refreshed cluster based on existing metrics
      &lt;/p&gt;
      &lt;SizingModeToggle /&gt;
    &lt;/header&gt;
    ```

    With logo added before the h1:
    ```tsx
    &lt;header className="mb-6 text-center print:hidden"&gt;
      &lt;img
        src="/presizion/logo.svg"
        alt="Presizion"
        className="mx-auto mb-3 h-8 w-auto"
      /&gt;
      &lt;h1 className="text-2xl font-bold tracking-tight"&gt;Cluster Refresh Sizing&lt;/h1&gt;
      &lt;p className="text-sm text-muted-foreground mt-1"&gt;
        Size your refreshed cluster based on existing metrics
      &lt;/p&gt;
      &lt;SizingModeToggle /&gt;
    &lt;/header&gt;
    ```

    Notes:
    - src="/presizion/logo.svg" uses the Vite base path prefix (app served from /presizion/)
    - h-8 w-auto renders at 32px height, width scales proportionally from SVG aspect ratio
    - mx-auto centers the image in the text-center container
    - mb-3 provides spacing between logo and heading
    - No new imports needed — img is a standard HTML element
    - File must remain ≤150 lines (currently 53, addition adds ~5 lines)
    - No TypeScript type changes needed
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/cluster-sizer && grep -q "presizion/logo.svg" src/components/wizard/WizardShell.tsx && rtk tsc --noEmit && echo "WizardShell OK"</automated>
  </verify>
  <done>WizardShell.tsx contains the logo img tag with src="/presizion/logo.svg". TypeScript compiles with no errors. Component is ≤150 lines.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of branding assets</name>
  <action>Human visually verifies that logo and favicon appear correctly in the running app.</action>
  <what-built>
    Created public/logo.svg (Presizion wordmark, blue/slate palette) and replaced public/favicon.svg with a Presizion geometric "P" mark. Updated WizardShell.tsx header to render the logo above the heading.
  </what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Open http://localhost:5173/presizion/ in a browser
    3. Verify: The browser tab shows a blue geometric "P" favicon (not the purple Vite bolt)
    4. Verify: The app header shows the Presizion logo image above "Cluster Refresh Sizing"
    5. Verify: Logo uses blue/slate colors (blue #3B82F6, no purple)
    6. Verify: Logo is readable and not distorted (h-8 height, proportional width)
    7. Verify: Remaining header elements (subtitle, SizingModeToggle) still appear below the logo
  </how-to-verify>
  <verify>Human visual confirmation via browser</verify>
  <done>Favicon shows blue Presizion "P" in tab. Header logo is visible, blue/slate palette, correctly sized.</done>
  <resume-signal>Type "approved" to continue, or describe any visual issues</resume-signal>
</task>

</tasks>

<verification>
1. `test -f public/logo.svg` — logo asset exists
2. `test -f public/favicon.svg` — favicon asset exists and contains Presizion mark
3. `grep -q "presizion/logo.svg" src/components/wizard/WizardShell.tsx` — logo wired in header
4. `rtk tsc --noEmit` — zero TypeScript errors
5. `rtk vitest run` — all tests still pass (no regressions from header change)
6. Visual: browser tab favicon is blue geometric P, not purple Vite bolt
7. Visual: header logo is visible above heading text
</verification>

<success_criteria>
- public/logo.svg: Presizion wordmark or "P" monogram, blue #3B82F6, slate #475569, valid SVG
- public/favicon.svg: Compact Presizion "P" mark, blue palette, valid SVG replacing Vite default
- WizardShell.tsx header: img tag with src="/presizion/logo.svg" renders above h1
- Zero TypeScript errors, zero test regressions
- Human verification confirms favicon and logo are visible and correct
</success_criteria>

<output>
After completion, create `.planning/phases/11-branding-and-tech-debt/11-02-SUMMARY.md`
</output>
