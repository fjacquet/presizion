---
phase: 30-step-2-scenario-cards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/step2/Step2Scenarios.tsx
  - src/components/step2/ScenarioCard.tsx
  - src/components/step2/ScenarioResults.tsx
  - src/components/step2/VsanGrowthSection.tsx
autonomous: false
requirements: [CARD-01, CARD-02, CARD-03, CARD-04, CARD-05]

must_haves:
  truths:
    - "At 390px, the Step 2 header (title + Add Scenario button) stacks vertically without clipping"
    - "At 390px, server config inputs display as single-column stack, each field full-width and readable"
    - "At 390px, sizing assumptions inputs display as single-column stack, each field full-width and readable"
    - "At 390px, vSAN settings fields display as single-column stack, readable and enterable"
    - "At 390px, Growth Projections fields display as single-column stack, readable and enterable"
    - "At 390px, ScenarioResults breakdown (CPU/RAM/Disk) stacks vertically with formula strings readable"
    - "At 390px, SPEC lookup search input and results panel are usable without horizontal page overflow"
  artifacts:
    - path: "src/components/step2/Step2Scenarios.tsx"
      provides: "Mobile-first header layout"
      contains: "flex-col"
    - path: "src/components/step2/ScenarioCard.tsx"
      provides: "Mobile-first server config and sizing assumption grids + touch-friendly card action buttons"
      contains: "grid-cols-1"
    - path: "src/components/step2/ScenarioResults.tsx"
      provides: "Mobile-first results breakdown grid"
      contains: "grid-cols-1"
    - path: "src/components/step2/VsanGrowthSection.tsx"
      provides: "Mobile-first vSAN and growth grids"
      contains: "grid-cols-1"
  key_links:
    - from: "src/components/step2/Step2Scenarios.tsx"
      to: "ScenarioCard + ScenarioResults"
      via: "Tabs container renders one card at a time"
      pattern: "TabsContent"
    - from: "src/components/step2/ScenarioCard.tsx"
      to: "VsanGrowthSection"
      via: "Child component with responsive grids"
      pattern: "VsanGrowthSection"
---

<objective>
Make Step 2 (Define Target Scenarios) fully usable on a 390px phone by applying mobile-first Tailwind grid patterns to all form grids, stacking the header, and ensuring card action buttons meet touch-target requirements.

Purpose: A presales engineer on a phone can define and review sizing scenarios without horizontal scrolling, clipped inputs, or cramped controls.
Output: 4 files with updated Tailwind classes -- no new components, no logic changes, no new dependencies.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-step-2-scenario-cards/30-RESEARCH.md

<interfaces>
<!-- Key component structure the executor needs. Extracted from codebase. -->

From src/components/step2/Step2Scenarios.tsx:
```tsx
// Line 18 -- header flex container (CARD-01 target)
<div className="flex items-center justify-between">
```

From src/components/step2/ScenarioCard.tsx:
```tsx
// Line 209-228 -- card action buttons (Duplicate + Remove) use size="sm"
<Button variant="outline" size="sm" onClick={() => duplicateScenario(scenarioId)}>
<Button variant="outline" size="sm" onClick={() => removeScenario(scenarioId)}>

// Line 237 -- server config grid (CARD-02 target)
<div className="grid grid-cols-2 sm:grid-cols-4 gap-4">

// Line 326 -- sizing assumptions grid (CARD-03 target)
<div className="grid grid-cols-2 sm:grid-cols-4 gap-4">
```

From src/components/step2/VsanGrowthSection.tsx:
```tsx
// Line 68 -- vSAN settings grid (CARD-04 target, first grid)
<div className="grid grid-cols-2 sm:grid-cols-3 gap-4">

// Line 223 -- growth projections grid (CARD-04 target, second grid)
<div className="grid grid-cols-3 gap-4">
```

From src/components/step2/ScenarioResults.tsx:
```tsx
// Line 77 -- results breakdown grid (CARD-01 readability)
<div className="grid grid-cols-3 gap-3 text-sm">
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply mobile-first grid classes to Step 2 components</name>
  <files>
    src/components/step2/Step2Scenarios.tsx,
    src/components/step2/ScenarioCard.tsx,
    src/components/step2/ScenarioResults.tsx,
    src/components/step2/VsanGrowthSection.tsx
  </files>
  <action>
Apply the mobile-first Tailwind grid pattern (established in Phase 28-29) to all Step 2 grids. Each change is a single className string replacement:

**Step2Scenarios.tsx (CARD-01):**
- Line 18: Change `className="flex items-center justify-between"` to `className="flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between"` -- this stacks the title and "Add Scenario" button on mobile.

**ScenarioCard.tsx (CARD-02, CARD-03, button touch targets):**
- Line 237 (Server Config grid): Change `grid grid-cols-2 sm:grid-cols-4` to `grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4` -- single-column at 390px, 2-col at sm, 4-col at md+.
- Line 326 (Sizing Assumptions grid): Same change -- `grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4`.
- Lines 210-227 (Duplicate + Remove buttons): Change both `size="sm"` to `size="icon"` and add `className="h-9 w-9"` to each button. This provides a larger touch target consistent with the Phase 29 ScopeBadge pattern. Keep the existing icon sizes (`h-3.5 w-3.5`).

**ScenarioResults.tsx (CARD-01 readability):**
- Line 77: Change `grid grid-cols-3 gap-3` to `grid grid-cols-1 sm:grid-cols-3 gap-3` -- formula strings get full width on mobile, preventing overflow of mono-spaced formula text.

**VsanGrowthSection.tsx (CARD-04):**
- Line 68 (vSAN settings grid): Change `grid grid-cols-2 sm:grid-cols-3` to `grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3` -- single-column at 390px.
- Line 223 (Growth projections grid): Change `grid grid-cols-3` to `grid grid-cols-1 sm:grid-cols-3` -- single-column at 390px, 3-col at sm+.

**What NOT to change:**
- Do NOT touch `src/lib/sizing/` -- these are purely visual changes
- Do NOT add `overflow-x: hidden` on any parent of SpecResultsPanel -- it must scroll horizontally
- Keep the aggressive mode amber banner OUTSIDE the grid div (lines 318-324) -- do not move it inside
- The HA Reserve toggle row (lines 388-416) already fits at 390px -- do not change
- The GHz mode section (lines 420-444) already uses `grid-cols-1 sm:grid-cols-2` -- do not change
  </action>
  <verify>
    <automated>rtk vitest run src/components/step2/__tests__/</automated>
  </verify>
  <done>
    - All 6 grid class changes applied across 4 files
    - Card action buttons updated to size="icon" with h-9 w-9
    - All existing step2 tests pass
    - No logic changes, no new components, no new dependencies
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Step 2 mobile layout at 390px</name>
  <files>n/a</files>
  <action>Human verifies all CARD-01 through CARD-05 requirements at 390px viewport in Chrome DevTools.</action>
  <what-built>Mobile-responsive Step 2 scenario cards with single-column grids at 390px, stacked header, touch-friendly card buttons, and responsive formula display. CARD-05 (SPEC lookup) was already complete from Phase 29 -- included in verification.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open Chrome DevTools (F12)
    2. Set device to iPhone 14 (390x844) in responsive mode
    3. Navigate to Step 2

    **CARD-01 -- Header stacking:**
    4. Verify "Step 2: Define Target Scenarios" title and "Add Scenario" button stack vertically (button below title)
    5. Verify neither is clipped or causes horizontal scroll

    **CARD-02 -- Server config grid:**
    6. Verify Sockets/Server, Cores/Socket, RAM/Server GB, Disk/Server GB each take full width, stacked vertically
    7. Verify all labels are fully readable (not truncated)

    **CARD-03 -- Sizing assumptions grid:**
    8. Scroll down to "Sizing Assumptions" section
    9. Verify all fields (vCPU:pCore Ratio, RAM/VM GB, Disk/VM GB, Headroom %, etc.) stack vertically at full width

    **CARD-04 -- vSAN and Growth section:**
    10. Click "vSAN and Growth" to expand
    11. Verify vSAN settings (FTT Policy, Compression, Slack %, etc.) stack vertically
    12. Verify Growth Projections (CPU/Memory/Storage Growth %) stack vertically
    13. Verify all fields are readable and enterable -- no clipping

    **CARD-05 -- SPEC lookup (already done):**
    14. If in vCPU mode, type a CPU model in "Look up target CPU" -- verify search input is full-width
    15. Verify results table is horizontally scrollable if it overflows -- no page-level scroll

    **Touch targets:**
    16. Verify Duplicate and Remove buttons in the card header are tappable without precision

    **ScenarioResults:**
    17. Enter some cluster data in Step 1, return to Step 2
    18. Verify the results area (servers required + CPU/RAM/Disk breakdown) stacks vertically with readable formula strings
  </how-to-verify>
  <verify>Visual inspection at 390px viewport in Chrome DevTools</verify>
  <done>All 5 CARD requirements verified visually at 390px -- no horizontal overflow, all fields readable and tappable</done>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
1. All Step 2 tests pass: `rtk vitest run src/components/step2/__tests__/`
2. Full test suite passes: `rtk vitest run`
3. Build succeeds: `npm run build`
4. Visual verification at 390px confirms all 5 CARD requirements met
</verification>

<success_criteria>
- All Step 2 form grids display as single-column at 390px viewport width
- Step 2 header stacks title above "Add Scenario" button on mobile
- Card action buttons meet touch-target requirements (h-9 w-9)
- ScenarioResults formula breakdown stacks vertically on mobile
- vSAN and Growth grids collapse to single-column on mobile
- SPEC lookup panel remains functional with horizontal scroll on mobile (already done, verified)
- No horizontal page-level scroll at 390px
- All 596+ existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/30-step-2-scenario-cards/30-01-SUMMARY.md`
</output>
