---
phase: 29-step-1-mobile-form-layout
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/step1/Step1CurrentCluster.tsx
  - src/components/step1/FileImportButton.tsx
  - src/components/step1/ScopeBadge.tsx
  - src/components/step1/DerivedMetricsPanel.tsx
  - src/components/step1/__tests__/FileImportButton.test.tsx
autonomous: true
requirements:
  - FORM-01
  - FORM-02
  - FORM-05

must_haves:
  truths:
    - "Step 1 header (title + import button) does not overflow at 390px"
    - "All CurrentClusterForm grids display single-column at 390px"
    - "DerivedMetricsPanel shows 2 columns at 390px, 5 at 640px+"
    - "FileImportButton is at least 44px tall for touch targets"
    - "ScopeBadge label does not overflow at 390px"
  artifacts:
    - path: "src/components/step1/Step1CurrentCluster.tsx"
      provides: "Responsive header layout (flex-col on mobile, flex-row on sm+)"
      contains: "flex-col"
    - path: "src/components/step1/FileImportButton.tsx"
      provides: "Touch-friendly import button with w-full on mobile"
      contains: "w-full sm:w-auto"
    - path: "src/components/step1/ScopeBadge.tsx"
      provides: "Truncated scope label with max-w on mobile"
      contains: "truncate"
    - path: "src/components/step1/__tests__/FileImportButton.test.tsx"
      provides: "Unit tests for FileImportButton rendering and accessible label"
      min_lines: 20
  key_links:
    - from: "src/components/step1/Step1CurrentCluster.tsx"
      to: "src/components/step1/FileImportButton.tsx"
      via: "JSX composition"
      pattern: "<FileImportButton"
    - from: "src/components/step1/Step1CurrentCluster.tsx"
      to: "src/components/step1/ScopeBadge.tsx"
      via: "JSX composition"
      pattern: "<ScopeBadge"
---

<objective>
Make the Step 1 header row, FileImportButton, ScopeBadge, and DerivedMetricsPanel responsive at 390px.

Purpose: FORM-01 (single-column form stacking), FORM-02 (DerivedMetrics 2-3 col on mobile), and FORM-05 (import button + scope badge readable/tappable at 390px) are the CSS-focused responsive fixes for Step 1 content that does not involve the ImportPreviewModal.

Output: Four modified component files with Tailwind responsive classes, one new test file for FileImportButton.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-step-1-mobile-form-layout/29-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/step1/Step1CurrentCluster.tsx (current layout):
```tsx
// Header row — needs flex-col on mobile:
<div className="flex items-start justify-between">
  <div>
    <h2 className="text-xl font-semibold">Step 1: Enter Current Cluster</h2>
    <p className="text-sm text-muted-foreground mt-1">...</p>
  </div>
  <FileImportButton />
</div>
```

From src/components/step1/FileImportButton.tsx (current button):
```tsx
// Button uses size="sm" (~32px) — needs size="default" for 44px:
<Button variant="outline" size="sm" onClick={...} disabled={loading} aria-label="Import from file">
  {loading ? 'Importing...' : 'Import from file'}
</Button>
```

From src/components/step1/ScopeBadge.tsx (current scope display):
```tsx
// Label span has no truncation — needs truncate + max-w on mobile:
<div className="flex items-center gap-2 text-sm text-muted-foreground">
  <span>
    <span className="font-medium text-foreground">Scope:</span>{' '}
    {activeLabels}
  </span>
  <Button variant="ghost" size="sm" onClick={openDialog} aria-label="Edit scope">
    <Pencil className="h-3.5 w-3.5" />
  </Button>
</div>
```

From src/components/step1/DerivedMetricsPanel.tsx (current grid):
```tsx
// Already uses grid-cols-2 sm:grid-cols-5 — verify this is acceptable:
<div className="grid grid-cols-2 sm:grid-cols-5 gap-4">
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Step1CurrentCluster header stacking + FileImportButton touch target + ScopeBadge truncation</name>
  <files>src/components/step1/Step1CurrentCluster.tsx, src/components/step1/FileImportButton.tsx, src/components/step1/ScopeBadge.tsx</files>
  <action>
    **Step1CurrentCluster.tsx** — Change the header div from `flex items-start justify-between` to `flex flex-col gap-2 sm:flex-row sm:items-start sm:justify-between`. This stacks the title block above the FileImportButton on mobile, side-by-side on sm+.

    **FileImportButton.tsx** — Three changes:
    1. Remove `size="sm"` from the Button (default size gives ~40px height, acceptable for 44px touch target per MOBILE-03 established in Phase 28)
    2. Add `className="w-full sm:w-auto"` to the Button so it stretches full-width when stacked below the header on mobile
    3. Keep `aria-label="Import from file"` unchanged

    **ScopeBadge.tsx** — Two changes:
    1. Change the outer div from `flex items-center gap-2` to `flex items-center gap-2 min-w-0` (allow flex children to shrink)
    2. Change the label span wrapping `activeLabels` to include `truncate max-w-[200px] sm:max-w-none` so long scope names get ellipsis on mobile but show fully on desktop
    3. Change the edit Button from `size="sm"` to `size="icon"` with explicit `h-9 w-9 shrink-0` to ensure it does not get pushed off-screen by a long label. Keep `aria-label="Edit scope"`.
  </action>
  <verify>
    <automated>rtk vitest run src/components/step1/__tests__/</automated>
  </verify>
  <done>
    - Step1CurrentCluster header stacks vertically on mobile with FileImportButton below the title
    - FileImportButton renders at default size (~40px) and full-width on mobile, auto-width on desktop
    - ScopeBadge truncates long labels with ellipsis at 200px on mobile, shows full label on desktop
    - All existing step1 tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: DerivedMetricsPanel verification + FileImportButton unit tests</name>
  <files>src/components/step1/DerivedMetricsPanel.tsx, src/components/step1/__tests__/FileImportButton.test.tsx</files>
  <action>
    **DerivedMetricsPanel.tsx** — Verify the grid already uses `grid-cols-2 sm:grid-cols-5`. Per research, this is correct — at 390px it renders 2 columns (each ~175px) which fits the metric labels and values. No change needed unless the label "vCPU:pCore Ratio" is being clipped. If it fits (it should at text-xs size for label), leave as-is. If the longest label causes overflow, change the MetricItem label from `text-xs` to `text-[11px]` but only if needed. Document verification in commit message.

    **FileImportButton.test.tsx** — Create new test file:
    ```
    - renders an "Import from file" button (getByRole('button', { name: /import from file/i }))
    - renders a hidden file input with accept=".xlsx,.csv,.zip,.json"
    - button has aria-label "Import from file"
    - button is not disabled by default
    ```
    Use the project's standard test patterns: `import { describe, it, expect, vi } from 'vitest'`, `import { render, screen } from '@testing-library/react'`. Mock the Zustand stores that FileImportButton's child ImportPreviewModal uses — use `vi.mock('@/store/useClusterStore')`, `vi.mock('@/store/useScenariosStore')`, `vi.mock('@/store/useImportStore')` with minimal mock returns.

    Note: FileImportButton itself does not import stores directly, but its child ImportPreviewModal does. Since ImportPreviewModal only renders when `importResult` state is non-null (and it starts null), the stores are not accessed during initial render — so a simple `vi.mock` with default returns is sufficient.
  </action>
  <verify>
    <automated>rtk vitest run src/components/step1/__tests__/FileImportButton.test.tsx</automated>
  </verify>
  <done>
    - DerivedMetricsPanel grid verified as correct at grid-cols-2 sm:grid-cols-5 (no change or minimal label size tweak)
    - FileImportButton.test.tsx exists with 4+ test cases covering rendering, file input, aria-label, and default enabled state
    - All tests pass
  </done>
</task>

</tasks>

<verification>
- `rtk vitest run src/components/step1/__tests__/` — all step1 tests pass
- `rtk vitest run` — full suite passes (no regression)
- `npx tsc -b` — no TypeScript errors
- Visual: At 390px viewport, Step1CurrentCluster header stacks vertically, FileImportButton is full-width, ScopeBadge label truncates with ellipsis
</verification>

<success_criteria>
- FORM-01: Step1CurrentCluster header stacks on mobile; CurrentClusterForm grids already mobile-first (verified)
- FORM-02: DerivedMetricsPanel grid confirmed as grid-cols-2 sm:grid-cols-5 (already correct)
- FORM-05: FileImportButton at 40px+ height with full-width on mobile; ScopeBadge label truncated with max-w-[200px]
- All tests pass including new FileImportButton.test.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/29-step-1-mobile-form-layout/29-01-SUMMARY.md`
</output>
