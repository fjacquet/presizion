---
phase: 07-enhanced-export-and-as-is-to-be-report
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/utils/__tests__/export.test.ts
  - src/__tests__/printCss.test.ts
  - src/components/step3/__tests__/ComparisonTable.test.tsx
  - src/components/step1/__tests__/CurrentClusterForm.test.tsx
autonomous: true
requirements: [EXPO-03, EXPO-04, REPT-01, REPT-02]

must_haves:
  truths:
    - "All four Phase 7 requirements have it.todo stubs in their respective test files"
    - "rtk vitest run exits 0 with stubs counted as pending (not failing)"
    - "No test file regressions — 222 existing tests remain green"
  artifacts:
    - path: "src/__tests__/printCss.test.ts"
      provides: "EXPO-04 automatable stub — checks index.css for print-color-adjust"
    - path: "src/lib/utils/__tests__/export.test.ts"
      provides: "EXPO-03 stubs for buildJsonContent and downloadJson"
    - path: "src/components/step3/__tests__/ComparisonTable.test.tsx"
      provides: "REPT-01 stubs for As-Is column rendering"
    - path: "src/components/step1/__tests__/CurrentClusterForm.test.tsx"
      provides: "REPT-02 stubs for unconditional existingServerCount and totalPcores auto-derive"
  key_links:
    - from: "src/__tests__/printCss.test.ts"
      to: "src/index.css"
      via: "fs.readFileSync string check"
      pattern: "print-color-adjust"
---

<objective>
Plant Nyquist Wave 0 test stubs for all four Phase 7 requirements — EXPO-03, EXPO-04, REPT-01, REPT-02 — before any implementation begins.

Purpose: Ensure the test suite exits 0 with pending stubs (it.todo, not it.skip), giving later implementation tasks a red/green target and satisfying the project's Nyquist sampling requirement.
Output: Four test files with it.todo stubs; one new file (printCss.test.ts); three extended files.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-enhanced-export-and-as-is-to-be-report/07-RESEARCH.md
@.planning/phases/07-enhanced-export-and-as-is-to-be-report/07-VALIDATION.md

<interfaces>
<!-- Key decisions from STATE.md governing test stub style -->
<!-- From [Phase 03-comparison-export-and-wizard-shell]: Nyquist Wave 0 stubs use it.todo (not it.skip)
     so Vitest counts them as pending not failing — test suite exits 0 -->

From src/lib/utils/__tests__/export.test.ts (existing file to extend):
```typescript
// Existing describe blocks: 'buildCsvContent', 'downloadCsv', csvEscape
// Add a new describe block for EXPO-03 stubs
describe('buildJsonContent', () => {
  it.todo('returns a valid JSON string')
  // ...
})
```

From src/__tests__/darkMode.test.ts (sibling file — same directory pattern):
```typescript
import { describe, it, expect } from 'vitest'
import { readFileSync } from 'node:fs'
import { resolve } from 'node:path'
// Pattern: read file contents as string, assert on string contains
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add EXPO-03 and EXPO-04 test stubs</name>
  <files>src/lib/utils/__tests__/export.test.ts, src/__tests__/printCss.test.ts</files>
  <behavior>
    - export.test.ts: new `describe('buildJsonContent')` block with it.todo stubs:
      * 'returns a valid pretty-printed JSON string'
      * 'includes currentCluster, scenarios, and computed results'
      * 'sets undefined optional fields to null (not omitted)'
      * 'includes schemaVersion: "1.1" at top level'
    - export.test.ts: new `describe('downloadJson')` block with it.todo stubs:
      * 'calls URL.createObjectURL with a Blob of type application/json'
      * 'calls URL.revokeObjectURL after download is triggered'
    - printCss.test.ts: new file in src/__tests__/ following the darkMode.test.ts pattern (readFileSync + string assertion):
      * `describe('index.css print rules')` with ONE real test (not todo): asserts the file contains 'print-color-adjust: exact'
      * This is the EXPO-04 automatable check; it fails now and will pass after Plan 03
  </behavior>
  <action>
    Extend src/lib/utils/__tests__/export.test.ts: append two new describe blocks after the existing `downloadCsv` block. Use it.todo (no callback) for all stubs in these blocks.

    Create src/__tests__/printCss.test.ts as a new file. Model it on src/__tests__/darkMode.test.ts — use node:fs readFileSync to read src/index.css as a string and assert it contains 'print-color-adjust: exact'. This single real test will be RED until Plan 03 adds the print CSS. That is intentional — it confirms the Wave 0 infrastructure is wired.

    Do NOT use it.skip — Vitest must count stubs as pending to keep suite exit code 0.
    Do NOT add implementation code in this plan.
  </action>
  <verify>
    <automated>rtk vitest run src/lib/utils/__tests__/export.test.ts src/__tests__/printCss.test.ts</automated>
  </verify>
  <done>export.test.ts shows additional it.todo entries for buildJsonContent and downloadJson; printCss.test.ts exists and its print-color-adjust assertion runs (may be red — that is OK at Wave 0)</done>
</task>

<task type="auto">
  <name>Task 2: Add REPT-01 and REPT-02 component test stubs</name>
  <files>src/components/step3/__tests__/ComparisonTable.test.tsx, src/components/step1/__tests__/CurrentClusterForm.test.tsx</files>
  <action>
    Extend src/components/step3/__tests__/ComparisonTable.test.tsx: add a new describe block (or extend the existing root describe) with it.todo stubs for REPT-01:
      - it.todo('renders an "As-Is" column header')
      - it.todo('As-Is column shows existingServerCount from useClusterStore')
      - it.todo('As-Is column shows "—" when existingServerCount is undefined')
      - it.todo('As-Is column shows sockets x cores server config string')
      - it.todo('As-Is column shows observed vCPU:pCore ratio')

    Extend src/components/step1/__tests__/CurrentClusterForm.test.tsx: add it.todo stubs for REPT-02:
      - it.todo('renders existingServerCount field when sizingMode is vcpu (unconditional)')
      - it.todo('renders existingServerCount field when sizingMode is specint')
      - it.todo('auto-derives totalPcores when existingServerCount, socketsPerServer, and coresPerSocket are all provided')
      - it.todo('does not override a manually entered totalPcores value')

    Do NOT use it.skip. Append stubs to the END of each file's describe blocks to avoid disturbing existing tests.
  </action>
  <verify>
    <automated>rtk vitest run src/components/step3/__tests__/ComparisonTable.test.tsx src/components/step1/__tests__/CurrentClusterForm.test.tsx</automated>
  </verify>
  <done>Both test files contain new it.todo stubs; existing tests in both files still pass; rtk vitest run exits 0</done>
</task>

</tasks>

<verification>
Run full suite after Wave 0:
`rtk vitest run --reporter=verbose`

Expected outcome: all existing 222 tests pass; new it.todo stubs appear as "pending"; printCss.test.ts assertion may be red (acceptable — it is the leading indicator for EXPO-04 implementation in Plan 03).
</verification>

<success_criteria>
- All four test files contain it.todo stubs for their respective requirements
- rtk vitest run exits 0 (pending stubs do not cause failure)
- 222 existing tests remain green
- printCss.test.ts exists in src/__tests__/ and tests that index.css contains print-color-adjust: exact
</success_criteria>

<output>
After completion, create `.planning/phases/07-enhanced-export-and-as-is-to-be-report/07-01-SUMMARY.md`
</output>
