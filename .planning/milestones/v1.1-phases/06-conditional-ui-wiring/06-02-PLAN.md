---
phase: 06-conditional-ui-wiring
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/components/step1/CurrentClusterForm.tsx
  - src/components/step1/__tests__/CurrentClusterForm.test.tsx
  - src/components/step2/ScenarioCard.tsx
  - src/components/step2/__tests__/ScenarioCard.test.tsx
  - src/components/step2/ScenarioResults.tsx
  - src/components/step3/ComparisonTable.tsx
  - src/components/step3/__tests__/ComparisonTable.test.tsx
autonomous: true
requirements:
  - PERF-02
  - PERF-03

must_haves:
  truths:
    - "When sizingMode is 'specint', CurrentClusterForm shows specintPerServer and existingServerCount fields"
    - "When sizingMode is 'vcpu', those two SPECint-only fields are hidden (unmounted, not CSS hidden)"
    - "CPU utilization % and RAM utilization % fields always appear in CurrentClusterForm (unconditional)"
    - "handleNext in CurrentClusterForm also validates specintPerServer when sizingMode is 'specint'"
    - "When sizingMode is 'specint', ScenarioCard shows a targetSpecint numeric field"
    - "ScenarioResults shows the SPECint formula row when sizingMode is 'specint' and all required params exist"
    - "ComparisonTable shows 'SPECint' (not 'Specint') in the limiting resource column"
    - "Full vitest suite exits 0; npm run build exits 0 with zero TypeScript errors"
  artifacts:
    - path: "src/components/step1/CurrentClusterForm.tsx"
      provides: "Conditional SPECint fields + unconditional utilization % fields + extended handleNext guard"
      contains: "sizingMode === 'specint'"
    - path: "src/components/step2/ScenarioCard.tsx"
      provides: "Conditional targetSpecint field when sizingMode is 'specint'"
      contains: "sizingMode === 'specint'"
    - path: "src/components/step2/ScenarioResults.tsx"
      provides: "Conditional SPECint formula row when mode is specint and params present"
      contains: "specintFormulaString"
    - path: "src/components/step3/ComparisonTable.tsx"
      provides: "RESOURCE_LABELS lookup replacing capitalize() for limitingResource display"
      contains: "RESOURCE_LABELS"
  key_links:
    - from: "src/components/step1/CurrentClusterForm.tsx"
      to: "src/store/useWizardStore.ts"
      via: "useWizardStore selector for sizingMode"
      pattern: "useWizardStore.*sizingMode"
    - from: "src/components/step2/ScenarioCard.tsx"
      to: "src/store/useWizardStore.ts"
      via: "useWizardStore selector for sizingMode"
      pattern: "useWizardStore.*sizingMode"
    - from: "src/components/step2/ScenarioResults.tsx"
      to: "src/lib/sizing/display.ts"
      via: "specintFormulaString import"
      pattern: "specintFormulaString"
    - from: "src/components/step3/ComparisonTable.tsx"
      to: "src/types/results.ts"
      via: "RESOURCE_LABELS Record<LimitingResource, string>"
      pattern: "RESOURCE_LABELS"
---

<objective>
Wire the Phase 5 formula engine into the existing UI: add conditional SPECint fields in Step 1 and each ScenarioCard, add always-visible utilization % fields in Step 1, extend navigation guards to block advance when SPECint fields are empty in SPECint mode, show the SPECint formula row in ScenarioResults, and fix the ComparisonTable capitalize bug for 'specint'.

Purpose: Complete PERF-02 and PERF-03 — users can now enter SPECint scores and see SPECint-based server counts and formula strings throughout the wizard.
Output: All conditional UI wiring in place; updated tests passing; build clean.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-conditional-ui-wiring/06-RESEARCH.md
@.planning/phases/06-conditional-ui-wiring/06-01-SUMMARY.md

@src/components/step1/CurrentClusterForm.tsx
@src/components/step2/ScenarioCard.tsx
@src/components/step2/ScenarioResults.tsx
@src/components/step3/ComparisonTable.tsx
@src/store/useWizardStore.ts
@src/schemas/currentClusterSchema.ts
@src/schemas/scenarioSchema.ts
@src/lib/sizing/display.ts
@src/types/results.ts
</context>

<interfaces>
<!-- Key types already available from prior phases -->
```typescript
// src/store/useWizardStore.ts
export type SizingMode = 'vcpu' | 'specint';
const sizingMode = useWizardStore((s) => s.sizingMode)

// src/schemas/currentClusterSchema.ts — fields already present in schema
// specintPerServer: optionalPositiveNumber (z.preprocess → number | undefined)
// cpuUtilizationPercent: optionalPercent (z.preprocess → number | undefined, min 0 max 100)
// ramUtilizationPercent: optionalPercent (z.preprocess → number | undefined, min 0 max 100)
// existingServerCount: optionalPositiveNumber (optional, used by SPECint formula)

// src/schemas/scenarioSchema.ts — field already present
// targetSpecint: optionalPositiveNumber (z.preprocess → number | undefined)

// src/lib/sizing/display.ts
export interface SpecintFormulaParams {
  existingServers: number;
  specintPerServer: number;
  headroomPercent: number;
  targetSpecint: number;
}
export function specintFormulaString(params: SpecintFormulaParams): string

// src/types/results.ts
export type LimitingResource = 'cpu' | 'ram' | 'disk' | 'specint';

// src/components/step2/ScenarioResults.tsx — RESOURCE_LABELS already defined
const RESOURCE_LABELS: Record<LimitingResource, string> = {
  cpu: 'CPU-limited',
  ram: 'RAM-limited',
  disk: 'Disk-limited',
  specint: 'SPECint-limited',
}

```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CurrentClusterForm — add SPECint + utilization fields + extend handleNext</name>
  <files>
    src/components/step1/CurrentClusterForm.tsx
    src/components/step1/__tests__/CurrentClusterForm.test.tsx
  </files>
  <behavior>
    CurrentClusterForm.test.tsx new test cases:
    - PERF-02: specintPerServer field visible when sizingMode is 'specint'
    - PERF-02: existingServerCount field visible when sizingMode is 'specint'
    - PERF-02: specintPerServer field NOT in DOM when sizingMode is 'vcpu'
    - PERF-02: Next blocked (onNext not called) when sizingMode='specint' and specintPerServer is empty
    - PERF-02: Next proceeds when sizingMode='specint' and specintPerServer has a valid value
    - SC-4: cpuUtilizationPercent field always visible regardless of sizingMode
    - SC-4: ramUtilizationPercent field always visible regardless of sizingMode

    Mock useWizardStore in each new test to control sizingMode.
  </behavior>
  <action>
    Modify src/components/step1/CurrentClusterForm.tsx:

    1. Add sizingMode selector at top of function body (after useForm declaration):
       const sizingMode = useWizardStore((s) => s.sizingMode)
       Import useWizardStore from '@/store/useWizardStore'.

    2. Add defaultValues for new fields in useForm call:
       specintPerServer: undefined,
       cpuUtilizationPercent: undefined,
       ramUtilizationPercent: undefined,
       existingServerCount: undefined,
       (These prevent RHF from treating them as uncontrolled)

    3. Add a "Current Utilization (optional)" section unconditionally after the existing server config section:
       - h3 with class "text-sm font-semibold text-muted-foreground uppercase tracking-wide mb-3"
       - Two NumericFormField in a 2-column grid:
         * name="cpuUtilizationPercent", label="CPU Utilization %", testId="input-cpuUtilizationPercent", optional
         * name="ramUtilizationPercent", label="RAM Utilization %", testId="input-ramUtilizationPercent", optional

    4. Add a conditional SPECint section rendered only when sizingMode === 'specint':
       {sizingMode === 'specint' && (
         <section> ... </section>
       )}
       Inside: h3 "SPECint Mode (required)", two fields in 2-column grid:
         * name="existingServerCount", label="Existing Server Count", testId="input-existingServerCount", optional
         * name="specintPerServer", label="SPECint/Server (existing)", testId="input-specintPerServer", optional

    5. Extend handleNext:
       async function handleNext() {
         const alwaysRequired: Array<keyof CurrentClusterInput> = ['totalVcpus', 'totalPcores', 'totalVms']
         const modeRequired: Array<keyof CurrentClusterInput> =
           sizingMode === 'specint' ? ['specintPerServer'] : []
         const isValid = await form.trigger([...alwaysRequired, ...modeRequired])
         // Extra check: in specint mode, specintPerServer must have a value (schema allows undefined)
         if (isValid && sizingMode === 'specint') {
           const specintVal = form.getValues('specintPerServer')
           if (!specintVal) return  // Block advance without calling onNext
         }
         if (isValid) onNext()
       }

    6. Extend CurrentClusterForm.test.tsx:
       - Import useWizardStore and add beforeEach reset for it
       - New describe block 'PERF-02: SPECint mode conditional fields'
       - New describe block 'SC-4: utilization percent fields'
       - Use vi.mocked(useWizardStore).mockReturnValue pattern or
         useWizardStore.setState({ sizingMode: 'specint' }) to control mode in tests
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/cluster-sizer && rtk vitest run src/components/step1/__tests__/CurrentClusterForm.test.tsx</automated>
  </verify>
  <done>
    All CurrentClusterForm tests pass including new PERF-02 and SC-4 cases;
    specintPerServer and existingServerCount only appear in DOM when sizingMode='specint';
    cpuUtilizationPercent and ramUtilizationPercent always appear;
    handleNext blocks when specintPerServer is empty in specint mode;
    vitest exits 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ScenarioCard + ScenarioResults conditional fields + ComparisonTable fix</name>
  <files>
    src/components/step2/ScenarioCard.tsx
    src/components/step2/__tests__/ScenarioCard.test.tsx
    src/components/step2/ScenarioResults.tsx
    src/components/step3/ComparisonTable.tsx
    src/components/step3/__tests__/ComparisonTable.test.tsx
  </files>
  <behavior>
    ScenarioCard.test.tsx new cases:
    - PERF-03: targetSpecint field visible when sizingMode is 'specint'
    - PERF-03: targetSpecint field NOT in DOM when sizingMode is 'vcpu'

    ComparisonTable.test.tsx (fill stubs from Wave 0):
    - 'shows SPECint (not Specint) in limiting resource column when specint is limiting resource'
    - 'shows CPU-limited label when cpu is limiting resource'

    ScenarioResults: no new automated tests — the SPECint formula row is tested visually (VALIDATION.md
    marks this as manual-only for layout). The conditional render logic is simple enough that
    integration tests via useScenariosResults (Phase 5 tests) cover the formula correctness.
  </behavior>
  <action>
    1. Modify src/components/step2/ScenarioCard.tsx:
       - Add at the top of the function body:
         const sizingMode = useWizardStore((s) => s.sizingMode)
         Import useWizardStore from '@/store/useWizardStore'.
       - Add conditional field inside the form, after the existing sizing assumptions section:
         {sizingMode === 'specint' && (
           <div className="mt-4 border-t pt-4">
             <p className="text-sm font-semibold text-muted-foreground uppercase tracking-wide mb-3">
               SPECint Target (required in SPECint mode)
             </p>
             <Controller
               control={form.control}
               name="targetSpecint"
               render={({ field, fieldState }) => (
                 <div className="space-y-1">
                   <Label htmlFor={`${scenarioId}-targetSpecint`}>Target SPECint/Server</Label>
                   <Input
                     id={`${scenarioId}-targetSpecint`}
                     type="number"
                     min={1}
                     data-testid={`input-targetSpecint-${scenarioId}`}
                     {...field}
                     value={field.value ?? ''}
                     onChange={(e) => field.onChange(e.target.value === '' ? '' : Number(e.target.value))}
                   />
                   {fieldState.error && (
                     <p className="text-sm text-destructive">{fieldState.error.message}</p>
                   )}
                 </div>
               )}
             />
           </div>
         )}
       - Note: use the existing form pattern (Controller + Input) rather than NumericFormField
         to avoid import complexity; ScenarioCard already uses this pattern for other fields.
       - Note: ScenarioCard already uses form.watch() → updateScenario via safeParse;
         targetSpecint is already in scenarioSchema so it will sync automatically when valid.

    2. Modify src/components/step2/ScenarioResults.tsx:
       - Add imports: useWizardStore from '@/store/useWizardStore', specintFormulaString from
         '@/lib/sizing/display'
       - Add sizingMode selector inside the component:
         const sizingMode = useWizardStore((s) => s.sizingMode)
       - Add SPECint formula row in the grid section (after disk row), conditionally:
         {sizingMode === 'specint' &&
          currentCluster.existingServerCount != null &&
          currentCluster.specintPerServer != null &&
          scenario.targetSpecint != null && (
           <div>
             <span className="text-muted-foreground">SPECint-limited: </span>
             <span className="font-medium tabular-nums">{result.cpuLimitedCount}</span>
             <div className="text-xs text-muted-foreground font-mono mt-0.5">
               {specintFormulaString({
                 existingServers: currentCluster.existingServerCount,
                 specintPerServer: currentCluster.specintPerServer,
                 headroomPercent: scenario.headroomPercent,
                 targetSpecint: scenario.targetSpecint,
               })}
             </div>
           </div>
         )}
       - Keep the grid as 3 columns always (cpu, ram, disk); the SPECint row renders below if present.
       - Change grid to auto-fit or wrap if needed: use className="grid grid-cols-3 gap-3 text-sm flex-wrap"
         or simply add the specint div outside the 3-col grid as a separate row.

    3. Modify src/components/step3/ComparisonTable.tsx:
       - Import LimitingResource type: import type { LimitingResource } from '@/types/results'
       - Add RESOURCE_LABELS constant (same as ScenarioResults — both components own it for locality):
         const RESOURCE_LABELS: Record<LimitingResource, string> = {
           cpu: 'CPU-limited',
           ram: 'RAM-limited',
           disk: 'Disk-limited',
           specint: 'SPECint',
         }
         Note: use 'SPECint' (not 'SPECint-limited') in ComparisonTable for brevity in the table cell.
       - Replace capitalize(result.limitingResource) with RESOURCE_LABELS[result.limitingResource]
       - The capitalize function can be removed if no longer used elsewhere in the file;
         if it is used elsewhere, keep it but stop using it for limitingResource.

    4. Fill ComparisonTable.test.tsx stubs:
       - Replace it.todo stubs with real tests
       - Render ComparisonTable with mock store state where limitingResource is 'specint'
       - Assert screen contains 'SPECint' and NOT 'Specint'
       - Also test limitingResource 'cpu' shows 'CPU-limited'

    5. Extend ScenarioCard.test.tsx:
       - Add describe block 'PERF-03: targetSpecint conditional field'
       - Test: targetSpecint input present when useWizardStore returns sizingMode='specint'
       - Test: targetSpecint input absent when useWizardStore returns sizingMode='vcpu'
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/cluster-sizer && rtk vitest run src/components/step2/__tests__/ src/components/step3/__tests__/</automated>
  </verify>
  <done>
    ScenarioCard shows targetSpecint field only in specint mode (tests pass);
    ComparisonTable shows 'SPECint' not 'Specint' for specint limiting resource (tests pass);
    ScenarioResults renders SPECint formula row when mode and params are present;
    Full suite: rtk vitest run exits 0;
    npm run build exits 0 with zero TypeScript errors
  </done>
</task>

</tasks>

<verification>
Full suite after plan completes:
  cd /Users/fjacquet/Projects/cluster-sizer && rtk vitest run --reporter=verbose

Expected: All pre-existing tests still green; new PERF-02/PERF-03 tests pass.

Build check:
  cd /Users/fjacquet/Projects/cluster-sizer && npm run build 2>&1 | tail -10

Expected: zero TypeScript errors; dist/ produced.
</verification>

<success_criteria>
- PERF-02: SPECint field and existingServerCount field appear in Step 1 only when sizingMode='specint'
- PERF-02: handleNext blocks advance when specintPerServer is empty in specint mode
- PERF-02: CPU and RAM utilization % fields always visible in Step 1
- PERF-03: targetSpecint field appears in ScenarioCard only when sizingMode='specint'
- PERF-03: ScenarioResults shows SPECint formula row when mode is specint and params are present
- PERF-03: ComparisonTable shows 'SPECint' (not 'Specint') in limiting resource column
- All tests pass including new PERF-02/PERF-03 test cases
- npm run build exits 0 with zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-conditional-ui-wiring/06-02-SUMMARY.md`
</output>
