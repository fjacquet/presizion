---
phase: 01-foundation
plan: 04
type: tdd
wave: 2
depends_on:
  - 01-02
files_modified:
  - src/lib/display/formulaStrings.ts
  - src/lib/display/__tests__/formulaStrings.test.ts
autonomous: true
requirements:
  - CALC-07

must_haves:
  truths:
    - "getCpuFormulaString returns a string containing the substituted numeric values"
    - "getRamFormulaString returns a string containing the substituted numeric values"
    - "getDiskFormulaString returns a string containing the substituted numeric values"
    - "Formula strings are human-readable (contain 'ceil', the inputs, and the result)"
    - "src/lib/display/ has zero React imports — pure TypeScript module"
    - "npx tsc --noEmit passes with zero errors"
  artifacts:
    - path: "src/lib/display/formulaStrings.ts"
      provides: "Three formula display functions for CALC-07 inline UI rendering"
      exports: ["getCpuFormulaString", "getRamFormulaString", "getDiskFormulaString"]
  key_links:
    - from: "src/lib/display/formulaStrings.ts"
      to: "src/types/cluster.ts"
      via: "optional import for parameter typing"
      pattern: "OldCluster|Scenario"
---

<objective>
Implement the formula display string module for CALC-07. Each function takes the same inputs as its corresponding formula function and returns a human-readable string showing the formula with substituted values — used by UI components to show transparent calculation logic inline.

Purpose: CALC-07 requires each key output value to display its formula and the specific input parameters used. This module isolates that presentation concern from the pure math in src/lib/sizing/.

Output: One source file and one passing test file. The display module is pure TypeScript with no React imports.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Formulas from Plan 02 that the display module mirrors -->

From src/lib/sizing/formulas.ts:

```typescript
// CALC-01: serverCountByCpu(totalVcpus, growthHeadroomFactor, targetVcpuToPCoreRatio, coresPerServer)
// CALC-02: serverCountByRam(totalVms, ramPerVmGb, growthHeadroomFactor, ramPerServerGb)
// CALC-03: serverCountByDisk(totalVms, diskPerVmGb, growthHeadroomFactor, diskPerServerGb)
```

The display functions take the SAME parameters as the formula functions and return human-readable strings.

Example output format (per RESEARCH.md and CONTEXT.md):

- getCpuFormulaString({ totalVcpus: 3200, headroomPercent: 20, targetVcpuToPCoreRatio: 4, coresPerServer: 40 })
  → "ceil(3200 × 1.20 / 4 / 40) = 24 servers"

- getRamFormulaString({ totalVms: 500, ramPerVmGb: 16, headroomPercent: 20, ramPerServerGb: 512 })
  → "ceil(500 × 16 × 1.20 / 512) = 19 servers"

- getDiskFormulaString({ totalVms: 200, diskPerVmGb: 500, headroomPercent: 20, diskPerServerGb: 10000 })
  → "ceil(200 × 500 × 1.20 / 10000) = 12 servers"
</interfaces>

</context>

<feature>
  <name>Formula display strings module (CALC-07)</name>
  <files>
    src/lib/display/formulaStrings.ts,
    src/lib/display/__tests__/formulaStrings.test.ts
  </files>
  <behavior>
    - getCpuFormulaString({ totalVcpus: 3200, headroomPercent: 20, targetVcpuToPCoreRatio: 4, coresPerServer: 40 }) returns string containing "3200", "1.20", "4", "40", and "24"
    - getRamFormulaString({ totalVms: 500, ramPerVmGb: 16, headroomPercent: 20, ramPerServerGb: 512 }) returns string containing "500", "16", "1.20", "512", and "19"
    - getDiskFormulaString({ totalVms: 200, diskPerVmGb: 500, headroomPercent: 20, diskPerServerGb: 10000 }) returns string containing "200", "500", "1.20", "10000", and "12"
    - All three functions return strings that contain "ceil" (formula is visible)
    - All three functions return strings that contain the word "servers" or the final count
  </behavior>
  <implementation>
    Use template literals. Compute the result inline (call serverCountByCpu/Ram/Disk or replicate the Math.ceil call) so the string shows the actual answer. The format must be readable when pasted into a slide or email.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Formula display strings (RED → GREEN → verify)</name>
  <files>
    src/lib/display/formulaStrings.ts,
    src/lib/display/__tests__/formulaStrings.test.ts
  </files>
  <behavior>
    - getCpuFormulaString output contains all five substituted values and "ceil"
    - getRamFormulaString output contains all four substituted values and "ceil"
    - getDiskFormulaString output contains all four substituted values and "ceil"
    - All functions return typeof string
    - No React imports anywhere in src/lib/display/
  </behavior>
  <action>
RED: Replace .todo stubs in formulaStrings.test.ts with real assertions. Import the three functions (they don't exist yet — TypeScript will error). Use `expect(result).toContain('3200')` style assertions since exact format has some discretion. Run `npx vitest run src/lib/display/` — must fail.

GREEN: Create src/lib/display/formulaStrings.ts:

Define parameter interfaces (or use inline object types):

```typescript
interface CpuFormulaParams {
  totalVcpus: number;
  headroomPercent: number;
  targetVcpuToPCoreRatio: number;
  coresPerServer: number;
}
// similar for Ram and Disk params
```

Implement each function using template literals:

- Compute `headroomFactor = 1 + headroomPercent / 100` — show it in the string as a decimal (e.g., 1.20)
- Compute the result using the same Math.ceil logic as the formula functions (can import from formulas.ts, or replicate the one-liner — importing is preferred to stay DRY)
- Format headroomFactor with `.toFixed(2)` for consistent display
- Return a string of the form: `"ceil(${totalVcpus} × ${headroomFactor.toFixed(2)} / ${targetVcpuToPCoreRatio} / ${coresPerServer}) = ${result} servers"`

ZERO React imports. This is a pure string-building module.

After implementation, run:
`npx vitest run src/lib/display/__tests__/formulaStrings.test.ts`
Must pass. Then run full suite:
`npx vitest run`
All tests must remain green.
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/cluster-sizer && npx vitest run src/lib/display/__tests__/formulaStrings.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>formulaStrings.test.ts all tests pass. src/lib/display/formulaStrings.ts exports getCpuFormulaString, getRamFormulaString, getDiskFormulaString. No React imports in src/lib/display/. npx tsc --noEmit zero errors.</done>
</task>

</tasks>

<verification>
After task completes:

1. `cd /Users/fjacquet/Projects/cluster-sizer && npx vitest run` — full suite green including formulaStrings tests
2. `npx tsc --noEmit` — zero errors
3. `grep -r "from 'react'" src/lib/` — must return nothing
4. Formula strings contain the substituted values and "ceil" keyword
</verification>

<success_criteria>

- formulaStrings.test.ts passes with zero failures
- Three exported functions produce human-readable formula strings with substituted values
- Output format shows "ceil(...)" and the computed server count
- No React imports in src/lib/display/
- Full Vitest suite still green after adding this plan's tests
- npx tsc --noEmit zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
